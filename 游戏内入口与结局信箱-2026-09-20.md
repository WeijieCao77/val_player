# 游戏内支持入口与结局信箱提示

本地实现，尚未发布。不混入已经锁定的发布批次；支持作者入口按作者要求不写游戏更新日志。

## 支持作者入口

首页仍保留原来的隐藏偏好；游戏中在更新日志旁常驻一个默认收起的支持图标，即使旧版本选择了“不再提示”也可主动打开。关闭或“收起支持面板”不会移除游戏入口。支持链接、二维码、金额说明不变。

`check_support_career_mobile.mjs`：320/390/1440 × 首页/生涯 × 无隐藏/当前隐藏/旧版隐藏，18 个场景通过。图标不重叠、不遮手机底栏，不自动弹出、不访问外网。

## 生涯结束后邀请提建议

新生涯结局完成、结局卡及阻塞弹窗关闭后，弹出一次可跳过的询问。可选“稍后再说”或“前往信箱”，也可关闭。不会自动填写、提交、投票。

进入信箱后不再被结局海报挡在下方；回到“本周”仍可查看结局。一次性标记随存档保存；已经展示过的不因重载重复弹，更新前已经结束的旧档不补弹。若浏览器无法保存，仍会走现有保存失败提示，不能保证丢失标记后跨重载不重复。

`check_ending_feedback.ts` 覆盖 6 条退役路径、新结局待提示、已提示重载、旧档静默和结局数据不变。`check_ending_feedback_ui.mjs` 使用真实 PlayerGame、结局卡与信箱，320/1280 × 旧档/稍后/前往信箱共 6 场景通过；仅主动进信箱才读取列表，没有投稿或投票。

上述检查与下一场轮次边界回归已接入候选 `check:feedback`。本地类型与边界检查通过；包含新信箱合并回执的整合 `check:feedback` 和 `npm run build` 均退出 0（`review-new-ui-feedback.log` / `review-new-ui-build.log`）。尚未上线，不把专项整合验证当作完整 `check:me`。
