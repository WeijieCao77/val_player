# 审美审查（val_player）

审查基线：`a1506a2`；分支 `taste2-a1506a2`。
样式（真正被加载的）：`src/main.tsx` → `src/ui/me/base.css`（令牌 + 底座，2649 行）、`src/me.css`（生涯层，698 行）；另有按屏加载的 `media.css` `hall.css` `moment.css` `rivals.css` `tour.css`。
**`src/styles.css` 没有任何地方 import**（经理模式的旧副本，与 base.css 只差 43 行）——审查它等于审查死代码，本文只针对 base.css / me.css。

约束（不可违反）：不新增运行时依赖、不引外部 CDN、不改屏幕 key / 导航文案 / 控件身份（今晚的埋点按它们做的 key）、不为了外观重构组件、不动 `src/engine/**`。

评级：**P0** = 坏掉或读不了；**P1** = 便宜且看得见；**P2** = 记下来不做。

---

## 0. 先查的那条线索：浅色 / 米色 下 1.0–1.3:1 的文字

**结论：作为「主题丢了令牌」的 bug 不成立；那个读数是量法本身的产物。下面是分辨过程。**

### 0.1 三套配色在令牌层是完整的（源码可证）

`src/ui/me/theme.ts` 的 `paintTheme()` 只做一件事：`document.documentElement.dataset.theme`，深色是「没有这个属性」。所以只有三种状态：无属性（深）、`light`、`cream`——**没有跟随系统的第四种状态**，也没有 `prefers-color-scheme` 分支（全仓只有 `prefers-reduced-motion` 的媒体查询）。

`base.css` 的三块：
- `:root`（第 19–98 行）：深色，**并且是唯一定义排版尺度、圆角、字体栈的地方**；
- `[data-theme="light"]`（第 104–152 行）、`[data-theme="cream"]`（第 158–206 行）：只覆盖颜色。

逐个令牌比对过：light / cream 覆盖了 `:root` 里全部 38 个颜色令牌，没有任何一个颜色**只**定义在 light 或 cream 里（那才是典型的失败模式）。反过来只定义在 `:root` 里的都是非颜色（`--radius` `--mono` `--sans` `--t-*` `--pick-w`），它们继承下去没有问题。
→ **不存在「某个令牌在某个主题下解析不出来」的情况**，所以 1.0:1 不可能来自这里。

### 0.2 1.28:1 是怎么量出来的

我第一版脚本在线上（`data-theme="light"`，这台浏览器自己存的配色，不是手改的）量到：

| 元素 | 报出来的底色 | 报出来的对比度 |
|---|---|---|
| `div.start-grid > button.start-card.on > span` | `rgb(217,42,57)` | **1.28** |

`.start-card.on` 的底色是 `me.css:335` 的 `background: var(--accent-wash)`，即 `rgba(217,42,57,.08)`——**半透明**。我的第一版脚本遇到 alpha>0 就直接把它当成不透明色返回，于是把 8% 的红洗拿去当成 100% 的正红来算。真实合成结果是 `--panel-2` 上压 8% 红 ≈ `rgb(242,229,232)`，配 `--muted #4e6378` 约 5.3:1，合格。

**这就是那条线索的来源：一个合成 alpha 的量法 bug，不是页面缺陷。** 任何把 `--accent-wash`、`--win-wash`、`--warn-wash`、`--panel-me` 这类洗色当成实色来量的脚本，都会在这三套配色下量出 1.0–1.5:1 的一片红字。修正脚本（逐层合成 alpha + 累计祖先 opacity）后的结果见 0.3。

### 0.3 修正后真正读不了的东西

见下面 P0-1：唯一一处真的接近 1.4:1 的文字，不是主题的锅，是 `button:disabled { opacity: .38 }` 压在「主按钮」上，而这个游戏把**下一步该干什么**写在了这颗禁用按钮的标签上。

---

## 1. P0（坏掉或读不了）

### P0-1 禁用的主按钮把「下一步该干什么」调暗到 1.87:1 —— 已修

**在哪**：`base.css:265` `button:disabled { opacity: .38 }`。
**为什么是 P0**：开局页的主按钮在条件没满足时是禁用的，而这个游戏**把缺什么写在了这颗按钮的标签上**（`NewCareer.tsx:286-289`：「先选一个出身」「还需分配 N 点天赋」）。线上实测（浅色，浏览器自己存的配色）：

| 元素 | 文字 | 实际对比度 |
|---|---|---|
| `div.newcareer > div.row.nc-go > button.primary`（禁用） | 先选一个出身 | **1.87:1** |
| `div.talent-row > div.row > button.sm`（禁用的 −） | − | 2.32:1 |
| `div.talent-tools > button.sm.ghost`（禁用的清空） | 清空 | 2.40:1 |

一个新玩家在开局页卡住时，页面上唯一告诉他卡在哪的那行字，是全页最看不清的字。

**改法**（`base.css`）：普通按钮的暗度从 `.38` 提到 `.62`（三套配色下禁用文字都回到 4.5:1 以上，且仍明显是「按不了」）；主按钮不再靠透明度，而是退掉红填充，用 `--panel-2` / `--line` / `--muted`——字读得清，又没有任何地方像是能按。深 / 浅 / 米三套配色下 `--muted` 压 `--panel-2` 分别是 4.8 / 5.6 / 5.7:1。

### P0-2 深色（默认配色）下所有主按钮是白字压 #FF4655 = 3.35:1 —— 已修（作者 2026-09-16 拍板选了方案 1）

**在哪**：`base.css:270` `button.primary { background: var(--accent); color: #fff }`，同样波及 `.seg button.on`、`.skip-link`、`.bgm-ib.main`、`.advance-main`。
**数字**：深色的 `--accent` 是 `#ff4655`，白字压上去 **3.35:1**。字号 14.5–16px / 字重 650，够不到 WCAG 的「大字」豁免（要 18.66px 且 700），所以 AA（4.5:1）不过。浅色的 `#d92a39` 是 4.84:1、米色的 `#cf2a38` 是 5.0:1，**两套浅底都是合格的——只有默认的深色不合格**。

**为什么我没有自己改**：#FF4655 是无畏契约的品牌红，作者在 `base.css` 开头写明了它「只属于品牌、主操作和自家队伍」，深底上的这抹红就是这个游戏的样子（「转播红」）。三条路都要拿品牌换合规，得作者拍板：

1. **只把「红当填充、白当字」的那几处加深**：新增 `--accent-fill`，深色下取 `#d4323f`（白字 4.85:1），品牌红继续管边框、文字、自家队伍。视觉上深色的主按钮会闷一档。
2. **深色下主按钮改深色字**：`#ff4655` 配近黑 `#17140d` 是 5.4:1，合规但完全不是这个游戏的样子。
3. **不改，记在案**：把它当作有意为之的品牌取舍（大量游戏 UI 这么做）。

我的建议是 1，但这是作者的决定，不是我的。

**作者当天拍板：走方案 1**（原话：品牌在边框、文字、「你的队伍」上活得比在按钮填充上多得多，「默认配色下每一颗主操作都不过 AA」不能为了好看留着）。已实现：新增 `--accent-fill` / `--accent-fill-hot`，深色 `#d4323f` / `#c22b38`，浅色与米色解析回各自的 `--accent`（那两套本来就过，一个像素不变）。改动只落在「填充底色 + 白字」的七处：`button.primary` 及 hover、`.seg button.on`、`.advance-main` 及 hover、`.skip-link`、`.bgm-ib.main` 及 hover、`.poster-band`（眉标是 13.5px 白字）。

## 2. P1（便宜、看得见）

### P1-1 桌面上的中文标签比手机上的还小 —— 已修

`:root` 的 `--t-label` 是 10.5px（≥1100px 时 11px），它管着 `th`、`.panel-head h2`、`.stat .k`、`.nav-group`、`.bracket-head` 这些标签。10.5px 是给拉丁字母的小标签定的尺寸；这个界面的标签全是中文——「从哪一年开始」「常规赛」「净胜局」——中文在 10.5px 上笔画已经并住了。

而 `me.css` 的手机段把生涯壳里的 `--t-label` 抬到了 **12px**，注释写着「守作者要的 12px」。于是出现了反过来的情况：**同一个标签，手机上 12px，桌面上 10.5px**——屏幕越大字越小。

改法：`--t-label` 10.5 → **11.5px**，≥1100px 的 11 → **12px**。手机段（生涯壳 12px）原样不动。顺带把四处写死在 12px 以下的中文小字换成 `var(--t-tiny)`：`.log-kind`（更新日志的「新增 / 调整 / 修复」）、`.unlock-body .k`（成就卡眉标）、`.rules-h`、`.ending .k`。

### P1-2 两张表没有自己的横向滚动盒 —— 报价卡那张：量出来了，已修

全站 20 张表里有 18 张裹在 `.table-wrap`（`overflow-x: auto`）里，这是对的。两张没有：

- `Week.tsx:381`「最近的比赛」（四列：日期 / 对手 / 比分 / 出场）
- `Modals.tsx:330` 报价弹窗里的合同表（四列两行：年限 / 年薪 / 签字费 / 违约金，金额可以很长）

前者四列都短，375 宽下没量到溢出；后者要走到一份真实报价才看得见。

**后来量到了（作者让我去证）。** 照着另一个 agent 的 `scripts/ending_preview.tsx` 的办法，写了 `scripts/offer_preview.tsx` + `offer-preview.html`：用自动托管把一条生涯跑到第一份报价落地的那一周就停住，把**真的**报价卡挂进 `.app.career`（手机上所有弹窗规则都挂在这个壳上，不套的话量到的是桌面弹窗）。375 宽下的真实报价「Twisted Saints Esports 的转会报价」：

| 量的东西 | 值 |
|---|---|
| 卡片正文可用宽 `.modal-body` | **358px** |
| 那张合同表实际宽 | **482px** |
| 宽出来 | **124px** |

它没有把整页推宽（`document.scrollWidth` 还是 375）——只是因为 `.modal` 上有 `overflow-y: auto`，按 CSS 规范 `overflow-x` 会跟着算成 `auto`，于是这张表**在卡片里横着跑，没有任何提示**：一份报价的年薪、签字费、违约金，有一半要横滑才看得见，而这正是作者已经报过 bug 的那张卡。

已修：在 `Modals.tsx` 里给它包一层 `div.table-wrap`（全站另外 18 张表都有的那一层），内联的 `margin: 12px 0` 挪到外层。不改任何 key、文案或控件身份。

**`Week.tsx:381` 那张仍然没量过**：四列都短（日期 / 对手 / 比分 / 出场，全 nowrap），估算比 358px 窄不少，但我没有实测，所以也没有动它。

### P1-3 开局页没有配色开关 —— 已修（作者 2026-09-16 拍板）

`ThemeToggle` 只挂在生涯壳的 `PlayerGame.tsx:402`（侧栏底部 / 手机的「更多」里）。开局页 `.newcareer` 在壳外面（`base.css` 里 `.ng-home` 的注释自己写明了这点），所以**一个觉得黑底看着晕的人，必须先开一局生涯才能换成米色**。修法是在开局页也挂一颗同样的 `ThemeToggle`——但那会给开局页添一个今晚埋点没见过的控件，是产品决定，不是我的。

**作者当天拍板：加**（今晚的埋点数的是屏幕和具体事件，多一个没埋点的控件不花什么）。已实现：开局页的两个视图（存档卡视图和建档视图）都在「成就殿堂」那一行的右端挂了现成的 `<ThemeToggle compact />`——不是新控件，桌面是深/浅/米三颗，手机自动换成它自带的单颗循环按钮。

## 2.5 已经是对的（查过，不用改）

审美清单里有一半的项目这套样式表已经做到了，写下来免得下一个人重查：

- **窄屏不横推**：375 宽的开局页 `document.scrollWidth === 375`，没有任何元素宽过视口。左右留边 10px（`.newcareer` 的 padding），面板再往里 0，不塌。
- **表格数字对齐 + 横向滚动收在盒子里**：`td.num / th.num` 带 `font-variant-numeric: tabular-nums` 和等宽字体；`.table-wrap { overflow-x: auto }`、`.bracket { overflow-x: auto }`、`.seg { overflow-x: auto }`。积分榜、选手榜的每一个数字列都挂了 `num`（`Standings.tsx` 里 20 多处，没有漏的）。
- **`prefers-reduced-motion`**：`base.css:245` 一条全局规则把所有 `animation-duration` / `transition-duration` 压到 .01ms，另有 6 处逐个 `animation: none`（`.unlock` `.poster` `.bgm-vinyl.spin` `.draw-step` `.update-nudge` `.cardface.r-mythic`），`me.css` 的弹幕、`tour.css` 的高亮框也各自关掉。**这一项是全仓做得最好的。**
- **焦点可见**：`:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px }` 是全局的，输入框另有 3px 的 ring。
- **一个强调色**：`--accent` 严格只用在品牌、主操作和「自己人」上；输赢用 `--win` / `--loss` / `--warn`，红色从不当「输」。三套配色各自把这些语义色压到底色上重新配过（浅色注释：「每一个彩色词都加深到白底上 4.5:1」）——查下来确实做到了。
- **触摸目标**：手机段 `button / select ≥ 40px`、导航 44px、`tr.clickable > td` 44px、模态关闭 44px。

## 3. P2（记下，不做）

- **`src/styles.css` 是死的**：2618 行完全没人 import，与 `src/ui/me/base.css` 只差 43 行。留着迟早有人改错文件（我这次就先读错了，白读了一千行）。删或留由作者定。
- **默认深色不跟随系统**：`theme.ts` 只有三种状态——没有属性（深）、`light`、`cream`，全仓没有一条 `prefers-color-scheme`。一台系统设成浅色的机器打开这个站，拿到的是深色。这不是 bug（作者写明深色是「这项运动自己的底色」），但它意味着**浅色用户要先自己找到那颗开关**，而开关又不在开局页（见 P1-3）。
- **不是所有东西都是卡片，但这里几乎都是**：`.act-card` `.save-tile` `.week-day` `.hall-sum > div` `.drill-card` `.origin-card` `.bracket-tie` `.market-box` `.preset-box` `.draw-group` 全是同一套「1px 边框 + 圆角 + `--panel-2` 底」，层级只靠边框色区分。一屏里七八个同样的盒子，眼睛分不出主次。改这个要动组件结构，不在这次的范围里。
- **间距不归布局所有**：`src/ui/me/*.tsx` 里有 80+ 处 `style={{ margin: '6px 0 0' }}` 这样的逐元素外边距，面板之间靠 `.panel { margin-bottom: 12px }`。同一种「说明小字」在不同屏上是 4 / 6 / 8 / 10px 的上边距。要改得逐屏重排，是重构不是调样式。
- **圆角有六个值**：`--radius: 3px` 是令牌，但 `.hero` 10px、`.week-day` 8px、`.cardface` 7px、`.save-tile` / `.club-set` 6px、`.pack-box` / `.draw-group` 5px、`.bgm` 999px 各写各的。
- **首页（`.home`）在手机上的标签还是 10px**：`--t-label` 的手机值只在 `.app.career` 和 `.newcareer` 两个壳里被抬到 12px，首页两个壳都不在。首页是经理模式共用的，这次没动。

---

## 4. 改完之后的复测

在本地 `vite build` + `vite preview` 的构建上重量（新 origin，localStorage 空 → 落到默认深色）：

- **禁用的主按钮**：`1.87:1` → **`4.89:1`**（`--muted` 压 `--panel-2`，`opacity: 1`），标签「先选一个出身」原样还在，按钮仍然一眼就是按不了的。
- **深色整页扫描**：开局页 101 处文字里，低于 4.5:1 的**只剩 2 处**，都是 `.seg button.on` 的白字压品牌红（3.36:1）——正是 P0-2 那件等作者定的事。除此之外深色全页过 AA。
- **浅色整页扫描**（同一份构建，用 app 自己的 `val_player.theme` 键切过去再刷新，`data-theme="light"`、body 底色 `rgb(238,241,245)`）：开局页**低于 4.5:1 的文字 0 处**。禁用的主按钮在浅色下是 `5.69:1`。
  → 也就是说：**那条「浅色/米色 1.0–1.3:1」的线索，在修好禁用按钮之后，浅色这边一处都不剩。**唯一还欠着的是深色的品牌红（P0-2）。
- **品牌红改完之后**（直接量三套配色的令牌，白字压填充色）：

  | 配色 | `--accent`（改前，白字压它） | `--accent-fill`（改后） | hover |
  |---|---|---|---|
  | **深色** | `#ff4655` — **3.36:1** | `#d4323f` — **4.85:1** | 5.68:1 |
  | 浅色 | `#d92a39` — 4.84:1 | 不变（解析回 `--accent`） | 6.0:1 |
  | 米色 | `#cf2a38` — 5.2:1 | 不变 | 6.42:1 |

  深色的主操作从不过 AA 变成过 AA，两套浅底一个像素都没动。
- **报价卡的合同表**（375 宽，真报价）：改前 `.modal-body` 358px 里塞着 482px 的表（`scrollWidth` 494 > `clientWidth` 358）；改后 **358 == 358，正文不再溢出**，那张表还是 482px，但它现在在自己的 `.table-wrap` 里横滑（`overflow-x: auto`，334px 视窗）。整页始终是 375，没被推宽过。
- **排版下限**：1265 宽下 `--t-label` 实测 **12px**（改之前是 11px），`.panel-head h2` 跟着到 12px；375 宽仍然 `scrollWidth === 375`、1265 宽 `scrollWidth === 1265`，没有新的横向溢出。`npm run typecheck` 和 `npx vite build` 都过（没跑整套 `check:me`，那个位子留给发布）。

---

## 附：怎么量的

- 线上（`https://valplayer-production.up.railway.app/`）量的是**浏览器自己存着的配色**（`data-theme="light"`，来自 `val_player.theme`），不是手改属性造出来的状态。
- 深色量的是本地 `npx vite build` + `vite preview` 的构建（新 origin，localStorage 空 → 落到默认深色，正是 `theme.ts` 里「深色是没有这个属性」的那条路径）。
- 对比度脚本逐层合成半透明底色并累乘祖先 `opacity` ——第一版没做这两件事，于是量出了 1.28:1 的假警报（见 0.2）。
