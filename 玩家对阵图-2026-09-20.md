# 玩家模式树状对阵图（待作者上线审批）

## 根因与范围

只读参考了 [Val_Manager 的 Bracket.tsx](https://github.com/WeijieCao77/Val_Manager/blob/main/src/ui/Bracket.tsx)。经理版按已生成 KO/SW 比赛分列，没有未来节点和连线。玩家版原来是其独立副本；真实历史赛历走 CircuitPanel，完全没有接入该组件，所以玩家看到的主要是排名表和折叠比赛记录。

本批只修改玩家展示，不改经理模式、赛程、比赛数值或存档。没有推送、部署。

## 现在显示什么

- 真实赛历：CircuitPanel 接入独立玩家对阵图，按真实节点来源画胜者实线、败者虚线；跨阶段名额注明来源，不凭队名猜连线。
- 使用 eventSoFar 的日期/世界线安全视图。未来历史比分、实际未来参赛队不直接从原始数据读取；模拟比赛显示本存档实际结果。轮空不伪造 0–0 比分；平局不把 B 方标成胜者。
- 循环赛与瑞士轮显示轮次配对，不画不存在的淘汰树。小组 GSL、双败、三败按引擎实际结构展示。
- 旧格式采用原引擎模板；单敗按已记录首轮和轮空配合原引擎首尾配对规则补后续节点。未来对手显示待定。
- 未来/已结束赛事整体默认折叠，进行中默认展开，避免全年赛事树同时撑长页面。320px 页面内独立横向滚动，末轮可到达。
- 业余杯的引擎只保存玩家自己的每轮结果，无法还原其他队伍完整签表。因此明确显示“你的晋级路线”，不捏造对手姓名和全场比赛；淘汰后的格子显示未晋级/未出场。

## 验证

- `npx tsx scripts/check_bracket_tree.ts`：2021–2027 共 588 个事件、14,467 个节点、10,981 条真实 w/l 边；验证未来不泄漏、历史日期切分、sim 胜负与比分、pending 对手、轮空无假比分、只读不改状态、无重叠/布局越界、旧双败/三败/GSL/单败轮空/瑞士平局。
- `node scripts/check_bracket_mobile.mjs`：真实组件和 CSS，320/390/1440 三个宽度 × future/history/swiss/legacy/cup/cup-done 六场景均通过。验证页面不横溢、组件内可滚动到末轮、赛报点击和弃权/淘汰路径；未读用户存档，外网请求全部拦截。
- `npm run typecheck`、`npx tsx scripts/check_boundary.ts` 通过。边界检查没有玩家对经理 UI 的依赖。
- 截图在 `.cache/bracket-mobile/`，已人工看 320px 旧双败和杯赛结果视图。

## 诚实边界

只有最终排名而没有比赛节点的历史公开资格赛，继续保留原排名/记录展示，不虚构全签表。历史回放节点没有可打开的本存档比赛报告；只有确有 fixture.result 的比赛卡可点击赛报。
