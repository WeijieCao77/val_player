# VAL Player 第二批本地更新候选

基线：`528cd9a`。目录：`.claude/worktrees/review-batch`。本文件记录本地工作，不代表已发布。

## 本批范围

### 信箱容量达到上限后无法清理

修前复现：临时 8 MB 日志，作者删除返回 303，内存和重启后的记录却仍存在。

现在遇到硬容量边界时，在副本上计算操作后的有效内容，以同目录临时文件和 rename 一次提交；只有成功后才更新内存。即使有效内容本身超过 8 MB，也允许真正减少数据的删除、合并和撤票逐步清理。写入失败返回 503 中文提示，不再假装成功。

保留坏尾隔离；失败追加后按实际文件大小更新容量计数。空快照保留非零标记，避免删光后重启重新出现预置建议。正常追加已成功、后续可选整理失败时，仍承认那次已经保存的操作。

新回归 `scripts/check_box_capacity.ts` 的 9 组测试通过，含半写故障、rename 故障、8 MB 以上有效快照、空信箱重启、回执票数和恢复投稿。原坏尾 5 组及 HTTP 信箱契约通过；另一代理独立复核未发现可复现缺陷。全部只用临时测试数据，没有读取或删除真实玩家建议。

限制：不保证断电 fsync 持久化；物理磁盘完全写不进临时快照时仍会失败并提示。普通追加“完整记录已写出但系统随后报错”的不确定提交边界仍存在。没有增加多进程共享日志协议。

### 2022 南美最后机会资格赛晋级席位错误

先前 role-five 分支的积分榜回归失败，实际揭露了基线和生产版本都已有的数据缺陷，而不是五人职责规则错误。

2022 冠军赛 `1015` 的两条南美 LCQ `1111` 路线都写成了 winner。第二席再次选中同一个冠军，被去重后退回历史原队 KRÜ；特定世界里应给 FURIA 的顺延资格丢失。

两条路线改为 top 的第 0/1 席。生成器只对已核实的 2022 南美双席做规范化，要求真实名次支持；未知重复 winner 不自作推断，而是报错。依据为 [Riot 官方冠军赛说明](https://valorantesports.com/en-US/news/valorant-champions-2022-everything-you-need-to-know) 的 S. America LCQ 1 / LCQ 2。

结构对比确认只改两条路线，未改名册、积分或其他年份赛制。`check_lcq_places.ts` 覆盖次名另路直通后的顺延、正常双席、历史回放和已抽签名单保持；生成器 4 项单测通过。生成器原有 32 条积分核对提示不是本批修复范围，不宣称历史数据全量无误。

旧档尚未抽签的对应赛事会使用修正路线；已抽签、进行中或结束的赛事不重新分配名额。不会追溯补发已错失的资格。

修后在 `role-five` 原分支上完整运行 `npx tsx scripts/check_standings.ts 11`，保持原种子和全部断言，通过：374 周、1560 次积分表、42 次抽签对照、3845 张赛事表、306 次名额顺延对照。原红灯已关闭；日志 `role-five/role-standings-fixed-full.log`。这不等于五人改革的所有集成/界面检查已完成。

## 手机界面验收

新增 `scripts/check_match_mobile.mjs`，真实 Chromium 渲染玩家模式 MatchModal 和正式 CSS，使用本地合成比赛，不访问线上、不写玩家存档、不启用遥测，浏览器启动即静音。

320、390、1440 px × BO1/BO3 共 6 场景通过：MVP 说明字号至少 12 px、没有横向越界、不被记分板横向滚动带走，切图与关闭正常，无 pageerror。主代理查看 320/390 截图确认文字可读，因此不为这项验收改动生产布局。

截图与测量 JSON：`.cache/match-mobile/`。这是记分板组件验收，不冒充整场比赛流程或所有页面全量验收。五人改革的“兼位”标签仍属于独立候选，未在本批验收。

## 集成验证

新增容量、晋级席位和记分板回归已接入 `check:me`；Python 生成器回归单独运行，不让网页运行或现有 Node 检查链依赖 Python。

源码提交 `a022216` 的单轮完整 `npm run check:me` 已通过（2026-09-20 10:41:48–11:09:28 EDT，27.7 分钟，退出码 0）；未删改断言。日志 `review2-check-me.log`。随后 `npm run build` 通过（退出码 0），日志 `review2-build.log`；Vite 仍提示既有大资源块，非构建失败。

上述结论只覆盖这批代码；后续选手资料纠错另行验证，不能自动继承本批全链通过的结论。上线前由作者审批；本轮不推送任何部署分支。

## 不混入本批的任务

- `bal-tune` 数值改革：独立做可追溯配对验证，不因少量样本盲调参数，也不并入本批缺陷补丁。
- 数值配对先导已完成：20/20 条、各 416 周到 2034 年、790 秒、无错误。固定 EMEA/2026/两种子，六组 SOLO 对照有五组终局不变，一组 1.8→1.6 反而 88→94，反映职业路径反馈。五位置差异值得进一步分析，但每位置两个样本不能代表总体；保持所有参数。详细口径、哈希和限制见 `bal-tune/数值先导矩阵-2026-09-20.md`，不把本先导称为整体难度达标或完整退役测试。
- `role-five` 五人职责改革：本批只提取其暴露的旧路由数据缺陷，不自动合入阵容改革。
- 创意工坊仍是策划范围，不实现。
- 卡牌肉鸽已独立到桌面“电竞卡牌肉鸽”，不作为 VAL Player 功能或发布内容。

## 首份合同等待：新一轮只读排查

没有证据支持直接放宽窗口。`prepro.ts` 的实际邀请池除了强度、名单人数、赛事资格，还排除已有邀请、窗口关闭和未来 112 天内解散的队伍；天梯到线也不等于综合的试训能力到线。

2021–2022 没有统一固定转会窗口，主要锁大赛；不能把所有旧样本的等待都解释成 Challengers 赛段锁名单。旧 `measure_first_hour.ts` 的可达时间只检查本区、reachableClubs、clubOpen，漏掉 foldingSoon 和已有邀请，而且只记首次可达，不证明之后一直有可用邀请池。

下一步应在真实 rollInvites 调用点逐层记录筛掉的队伍数量、实际概率和命中、窗口解锁日，且不额外消耗 RNG；将“能力未到 / 没有可用队伍 / 有队伍但未命中 / 已有邀请 / 试训失败”分开统计。本次仅审查代码，没有把这些原因冒充已完成的生涯实测，也没有改门槛或窗口。
