# 代码复查与待办 · 2026-09-20

本轮以线上 `8c2b8a0` 为基线，接续数值分支并检查玩家模式的存档、周行动撤回、经济、信箱、部署和测试边界。
这是有复现和回归的重点审查，不是“每一行都已审过”或完整安全认证。旧交接文档中的完成状态应以下表和本轮最终验证为准。

## 本轮确认的问题

| 问题 | 证据与处理 |
|---|---|
| 评分回合数没有真正封顶 | 独立候选 `bal-tune@341866f` 已修。旧算法把单人回合数 1500 改为 3000，会同时改变三人的评分；修为全流程共用封顶权重，赛事背景参与排名，不按名字压分。详见该分支《数值核验-2026-09-20》。 |
| 购买后撤回训练可免费拿商品 | 训练后买外设/语言课再撤回，金钱从 20000→9000→20000，但物品仍在。成功的经济操作现在封定此前行动；之后新做的行动仍可撤回，失败购买不封定。13 类操作回归通过。 |
| 信箱坏尾吞掉成功投稿 | `box.jsonl` 尾部半条 JSON 没换行，下一次成功写入被粘在同一坏行，重启消失。每次追加前后都加换行，隔离残片；覆盖启动已有坏尾及同进程追加中途失败。不在启动时截断原文件。 |
| 多标签页异步保存覆盖 | 旧页通过归属检查后等待数据库，新页接管并保存，旧页随后仍可能覆盖新档和归属记录。已加入事务实际拿锁时的校验、提交后二验、同页重开同生涯的代数保护；退出保存提交后才确认成功，导入失败不能夺回其他页的归属。 |

经济修复不无差别清除旧存档中的周初快照；升级前已产生的异常快照不能完整逆推出购买历史。
存档修复覆盖已复现的异步竞争窗口；原同步 localStorage 归属协议仍非跨进程原子 CAS，不能宣称所有极端并发时序已形式化消除。
信箱修复解决后续成功记录被坏尾吞掉，不自动恢复以前已经粘在坏行中的投稿，也不承诺断电持久化或失败追加的原子回滚：完整 JSON 已写出但调用报错时，重启可能仍重放它，这是原存储格式的既有边界。

## 原有任务的当前分类

### 已上线，不必重复做

- `76929cf`：大存档接入 IndexedDB，生涯内可导出内存中的最新进度。电脑的空闲内存不等于浏览器站点存储额度。
- `8c2b8a0`：移除网页标题、游戏界面与封面中的 Demo 标识。
- 前批即时行动、自动更新回到原生涯、信箱审核、同联赛转会归属、首份合同提速、MVP 说明等已有生产合并记录。

### 仍需后续处理或更大样本验证

1. **五人位置改革 `role-five` 未上线。** `6563e17` 的积分榜检查中，2022 南美 LCQ 名额出现 FURIA/KRÜ 分歧。须先查清资格/同分排序和检查模型，不能视为“随机结果变化”直接忽略。
2. **数值扩展验证。** SOLO 1.8/1.6 的三起点配对比较、所有位置与出身覆盖、完整退役及长期夺冠率仍未完成。8×8 指八条八赛季，不等于完整退役样本；赛区和出身不能混为因果。
3. **首份合同目标仍在上沿。** 旧 240 条样本中位 16.1/16.3 周，略超过 14–16 周目标；先核对俱乐部可用名单和转会窗口，不能为提速编造赛制。
4. **界面验收。** 手机记分板的 MVP 说明、兼位标签仍需实际窄屏检查；未验收不等于已经确认有 bug。

### 等作者决定

- 外援规则是否开启，以及国籍缺失和常驻资格怎样处理。未重新统计当前缺失比例，不沿用旧数字作结论。
- 2021 港澳台开局进入 2023 联赛结构后的主场归属。
- 创意工坊的内容类型、审核与数值边界：目前仅讨论策划，没有开发授权，也没有相关代码改动。

### 明确延后

2022 EMEA 区域数据补齐、跨赛季升降级、NoNamers 同名战队处理、legacySeeds 覆盖及 CI 建设。它们不应混进本轮缺陷补丁。

## 工程改进建议

- 保留本轮新增的精准回归并挂到完整检查链，尤其是多标签异步时序、坏尾重放和经济撤回。仅测正常保存/购买/投稿抓不到这些组合问题。
- 后续模拟结果应附 Git 提交、种子、起点、位置、参数及采样终点。旧 `scratch-career-mile*.json` 没有完整参数溯源，不能当成严格受控实验。
- CI 后续先接类型检查、边界检查和短回归，长生涯模拟单独运行。不要并发跑多条高内存模拟。
- 保持改动聚焦；没有为“整理代码”重写整个游戏或接入经理模式。

## 验证记录

- 生产依赖 `npm audit --omit=dev`：0 个已知漏洞（检查时 registry 结果，不等于应用没有漏洞）。
- 玩家依赖边界：通过，233 个可达模块（232 个运行时模块），首页 68 个模块，禁止依赖 0、提前加载数据集 0。
- 信箱原有 HTTP 契约/审核/限流/转义/硬杀重启检查与新增坏尾测试：通过。
- 经济 13 类操作及既有即时周行动检查：通过。
- 真实 Chromium 同源双页 8 种存档交错：通过；同测试在 `8c2b8a0` 首例即复现 A 覆盖 B。旧版存档容量、多标签、备份导入检查和类型检查亦通过，断言没有削弱。
- 独立数值候选 `341866f`：480 次封顶不变性、类型、IGL、迁移 13 条、世界线、8×8 数值回归 37 条均通过。但终局排序为 82/84/84/86/86/89/89/90，中位 86，没有达到产品目标 87–89；不能把代码回归通过说成难度已达标。世界线夹具从 10 改为新旧版本都能触发目标情形的 4，断言未改。
- 最终集成全链：待本轮运行结果补记。

简单清单整理和坏尾方案复核使用了 DeepSeek `deepseek-v4-pro` 高级推理；其输出经主代理核对，源码修复和集成结果以实际测试为准。没有把 API key、玩家存档或信箱真实内容发给模型。
