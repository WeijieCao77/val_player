{
  "name": "val-player",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "无畏契约（VALORANT）选手生涯模拟器 — a browser-based VALORANT pro-player career simulator",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "check:me": "npm run typecheck && npx tsx scripts/check_boundary.ts && npx tsx scripts/check_era.ts && npx tsx scripts/check_money.ts 3 7 && npx tsx scripts/check_ceremony.ts 4 7 && npx tsx scripts/check_clout.ts 6 7 && npx tsx scripts/check_tryout.ts 4 && npx tsx scripts/check_bottleneck.ts 6 7 && npx tsx scripts/check_injury.ts 3 7 && npx tsx scripts/check_story.ts 3 && npx tsx scripts/check_hall.ts && npx tsx scripts/check_circuit.ts 11 && npx tsx scripts/check_seats.ts 11",
    "check:me:full": "npm run check:me && npx tsx scripts/check_tryout.ts 8 && npx tsx scripts/check_career_end.ts 6 && npx tsx scripts/check_pool.ts 12 7",
    "start": "node server.js"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.4.0",
    "esbuild": "^0.25.12",
    "playwright": "^1.63.0",
    "tsx": "^4.23.12",
    "typescript": "^5.8.0",
    "vite": "^6.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
