node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
*.log

# never commit API keys
.env
.env.local

# python bytecode
__pycache__/
*.pyc

# scrape caches: raw vlr/liquipedia responses, rebuildable
scripts/cache/

# local experiments, not the game's build
dist-minitool/
val-manager-minitool.zip

# the server-side engine bundle, built by `npm run build:server`
dist-server

# TypeScript incremental build state
tsconfig.tsbuildinfo

# scraped vlr.gg pages (18 MB); regenerate with scripts/fetch_history.py
.cache

# 13.5 MB of raw per-event statlines; rebuild with scripts/fetch_stats.py
# (the aggregate scripts/build_stats.py produces IS committed)
src/data/stats_history.json
