# 玩家成长与数值平衡 · 第一轮

基线：已上线 `3addd0f`。本地候选分支 `codex/growth-balance`。作者最新授权：休息期间完成验收，确认无问题即可推送上线，不必再等待批准。目标是适当降低难度，重点缓解队内冲突与路人开局长期滞留次级联赛；仍保留比 LOL Breaker 更丰富的成长、岗位、试训与注册限制。

## 范围

先分离两个问题：玩家成长节奏，以及真实选手的评分合理性。旧 `bal-tune` 同时改变年龄、上限、训练、指挥加成和世界评分，不能整包混入最新生产版；尤其不能撤销已经上线的托管、存档、地图、资料、信箱修复。

第一项受控实验只测玩家个人训练的属性余量减速：`me/growth.ts ROOM_SCALE 10 → 8`，保留最低倍率0.25、最高1.3。实验时只在探针内存生效；完成20条对照后采用到本轮候选代码，尚需合并验收。

- 离上限5点的个人训练倍率由0.50变为0.625；离上限8点由0.80变为1.00。
- 最后1–2点仍为0.25；余量13点及以上仍为1.3；达到上限后仍不囤积经验。
- 不改上限、年龄、天赋权重、奖杯奖励、行动点、疲劳、比赛胜率或AI评分。
- 这只影响玩家个人行动，不宣称俱乐部周训练也同步加速，更不能把局部25%倍率变化说成整条生涯成长加快25%。

## 测量口径

同一份生产源码，五位置 × 种子7001/7212 × 原规则/候选，共20条；2026年、EMEA、Challengers起点、网吧常客 `netcafe`、20点均衡天赋、同一托管策略。原计划4赛季；建档实测余量15–17点，初期两规则完全一样，因此正式对照扩为8赛季至2034年，记录每年属性/经验/上限、行动投入、出场/伤病情况及首次达到80/85/90的时间。

两种子每位置只用于诊断，不能代表所有玩家或证明最终难度达标。旧 `bal-tune` 的20条八赛季不是当前线上基线；它使用的 `net` 是无效键并回退到网吧出身，且 `emptyTalents()` 不是零天赋。

## 已发现的后续整合风险

1. 旧数值候选与线上都是 `RULER=2`，当前老档会被迁移函数跳过；不能仅加版本号后重复套用旧位移。
2. 提高新建档的 `CAP_BASE` 不会自动提高老档已有的属性上限，需要单独的公平、幂等迁移设计。
3. 指挥加成需同步处理任命、卸任、离队、读档，不能只给计算公式加3。
4. 旧年龄方案包含反应24岁、枪法25岁开始下降，33岁训练归零；这是独立的规则改变，不能用“成长变快”来概括。

## 执行记录

- 基线 `npm run typecheck` 退出0。
- 新探针静态检查、plan（零模拟）与两版本各一周smoke通过；动作和初始状态相同。
- 正式命令：`node --expose-gc scripts/probe_growth_current.mjs run .cache/growth-paired-8y-20260920.jsonl 8`。
- 主agent串行跑完20条，exec session `48635`实际退出0。
- `scripts/check_growth_room.ts` 初次在生产10上得到预期曲线红测；采用8后11组通过，补小数边界后17组通过。已加入正式检查链，不能把最初用于确认差异的两项红测当生产bug。

## 第一项实验完成

主 session 48635 已收取退出码0；20/20条均完成416周至2034边界，未提前退役。源码保持不变，结果保存在本机 `.cache/growth-paired-8y-20260920.jsonl`。分析脚本 `scripts/summarize_growth_current.mjs` 完整性、上限与经验守卫全部通过。

八赛季综合数值中位数84.5→85.5，10组配对中6升、1平、3降；所有配对首次达到80都提前，中位周数241.5→214。达到85为5/10→7/10，90为0/10→1/10。夺冠与履历反馈会放大或抵消直接训练收益，不能说所有玩家最终数值都提高，亦不能拿不同达标人群的85中位数当配对加速率。

已采用玩家个人训练 `ROOM_SCALE=8`，针对性11组测试通过。此前“内存候选/尚未采用”记录为实验过程，不再代表当前状态。旧档不追溯加属性、不改上限，下一次训练读取新曲线。

## 合并候选仍待验收

- 托管试训：合法次级→一级升级不再被“目标队评分至少高2点”错误挡住，技能、试训、合同与注册规则仍保留。
- 队内冲突：仅玩家生涯自己的俱乐部受控频率，经理模式保持原行为；定向限频/关系阈值测试已通过。
- 兼容测试抓到并修复生产已有的读档行动点重复退款：以已执行非对位行动的记录识别即时格式，旧未执行计划仍退款一次。新测试8组、原周内即时行动测试、真实生产旧档跨版读取/撤回/再次保存幂等通过。
- seed701/7212的真实经理队及全NPC周训练与生产基线完整状态逐字节一致；成长调整不外溢到经理/NPC训练。
- 接下来跑路人开局跨年份、地区与天赋的配对矩阵，并执行完整回归、构建和正式主站部署核验。当前没有将成长反馈标为已修复。

### 第一版合并对照完成，临时替补补漏

`2107e24` 的16条路人生涯已跑完，409.54秒退出0，完整性分析通过。玩家相关争执58→19、宿怨提醒34→8（职业周3197→3199）；次级联赛总周数1331→1173（六组减少，两组持平）；八赛季综合均值84.00→85.88（五升、两平、一降）。首次进入一级队日期八组相同，不宣称第一次晋级普遍提前；两版8/8均有真实一级队首发，不能从这些样本推断所有路人都能晋级。

独立复核还找到伤缺临时替补保留外部teamId时绕过限频的问题。修复以“玩家己方这场比赛”决定事故预算，而不是每对球员所属队；真实临时替补复现4条→1条，27天不重复/28天可再次触发。旧基线同一career阵容的10对关系值和RNG完全一致，经理与非己方完整状态仍字节一致。补漏后的最终候选将再做对照；首16条不冒充最终版数据。

## DeepSeek 分工

已实际调用 `deepseek-v4-pro`（thinking/high）做测试边界清单与首轮结果摘要。只发送规则说明及合成实验汇总，不发送密钥或真实玩家投稿。主代理复核数值，纠正其把八赛季截点泛称“结局”的措辞；部署判断由完整验收决定，不交给摘要模型。

更新日志草案也交给v4-pro；两次思考模式返回不完整，未采用，随后关闭思考模式的同款高级模型返回完整草案（399 tokens），主代理修正文案并补上后来发现的读档行动点修复。不会把API调用或草案生成称为代码已验收。

## 与 LOL Breaker 的对照边界

已只读核对 GitHub [LOL_breaker main 的 2d21f48](https://github.com/WeijieCao77/LOL_breaker/tree/2d21f48460ed2985373d480c34236f00c031faf7)，没有修改该项目或启动其游戏进程。

- [成长公式](https://github.com/WeijieCao77/LOL_breaker/blob/2d21f48460ed2985373d480c34236f00c031faf7/demo/src/main.ts#L3211)的贴顶额外减速主要在最后5点；VAL本轮仍保留更长的余量减速段。两款属性维度、权重和训练基础不同，不能拿相同综合数值直接比较难度。
- [职业邀约](https://github.com/WeijieCao77/LOL_breaker/blob/2d21f48460ed2985373d480c34236f00c031faf7/demo/src/tryout.ts#L865)对优秀表现连续空窗有较强保底，LDL表现足够时明确进入一线邀约池。本轮不照搬92%/96%等概率，也不保证升一级；VAL仍须赛事表现、位置空缺、试训与注册规则，先移除托管误拒这一实际障碍。
- [更衣室事件](https://github.com/WeijieCao77/LOL_breaker/blob/2d21f48460ed2985373d480c34236f00c031faf7/demo/src/team.ts#L170)同类事件有赛段去重。VAL的问题并非“存在队友关系”，而是赛后争执、周宿怨、矛盾剧情叠加过密，所以限制重复惩罚，而不删除关系系统。

“比 LOL Breaker 难一点”是本轮设计取向，不是跨游戏实测已经证明的精确排序。验收可以证明相对VAL线上基线的变化，不能把少量模拟说成覆盖所有打法。
