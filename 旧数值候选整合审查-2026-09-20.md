# 旧数值候选整合审查 · 2026-09-20

审查范围：旧数值候选 `bal-tune/150ea3c751f734ba2123fab0e686b628acc9487b`，对比已上线 `review-batch/3addd0f0581590a6ca7bb22f8013058a94f2d56b`。共同祖先为 `76929cf48b3460cf943a075592e5430a6f2a0acf`。

本审查只读取源码、Git 差异、接手说明、数值核验与先导矩阵报告；没有运行模拟或构建，没有修改游戏代码，没有提交、推送或操作线上信箱。此文是审查记录，不是实施或发布证明。

**当前实施边界：第一项实验仅比较 `growth.ts` 的 `ROOM_SCALE 10 → 8`。** 不同时修改年龄曲线、SOLO、CAP_BASE、奖杯奖励、世界评分或AI成长。尺子修复后续独立移植、独立验证。旧年龄方案来自作者此前的历史设计要求，但本轮不能将“更早衰退”混称为“成长加速”。

## 总体结论

应从 `3addd0f` 精确移植选中的数值实现，不能整树覆盖为 `150ea3c`。两树相差130个文件；旧数值分支缺失大量已经上线的修复。

旧20条八季矩阵只描述数值源码 `341866f`、旧托管逻辑与当时资料。它不是当前线上成长基线，更不能用旧中位86或旧检查全绿证明新整合候选。

## 可复用的实现与依赖

下列8个已有源码文件在共同祖先 `76929cf → 3addd0f` 之间没有生产修改；加上新增 `age.ts` 共9个文件。其数值补丁没有覆盖这些文件中后续生产修复的冲突，但这不代表可以不经重新验证直接发布。

| 文件 | 可复用内容 | 整合注意 |
|---|---|---|
| `src/engine/ruler.ts` | 有效回合先封顶1500；赛事参赛场强度先验与排名补足；2023无联赛席位的一级赛事分层 | 场强数据是报名阵容，不是逐场实际对手；保留此限制。当前不与成长实验混测 |
| `src/engine/timeline.ts` | 队友参与重评、`ratingStep` 年度限幅、造世界使用 `atOnce` | 真实生涯限幅与造世界快进要分别验证 |
| `src/engine/age.ts` | 统一训练年龄曲线、属性拐点、衰退与勤练缓冲 | 历史设计方案，不是本轮单纯加速补丁 |
| `src/engine/training.ts` | 表现证据影响成长、潜力重评，接入统一年龄规则 | 同时改变世界对手与玩家竞争环境，不能与个人成长归因混淆 |
| `src/engine/player.ts` | `IGL_BONUS/baseBonus`、年龄函数出口 | 指挥身份改变后的综合与上限缓存需要补验 |
| `src/engine/me/growth.ts` | 统一年龄曲线与提示、命名后的SOLO | 本轮第一实验只动ROOM_SCALE，其他部分保留线上行为 |
| `src/engine/me/life.ts` | 属性衰退拐点的生活提示 | 应与实际启用的年龄规则同步，不提前显示未启用规则 |
| `src/engine/me/bottleneck.ts` | 上限与突破奖励、指挥加成口径 | 奖励改动是平衡政策，不是纯缺陷修复；旧档补发需单独决定 |
| `src/engine/me/talent.ts` | CAP_BASE与预览中的指挥加成类型口径 | CAP_BASE改动不会自动重算已有玩家的caps |

相关专用检查可复用其测试意图：`check_ruler_evidence.ts` 的整张评分表封顶不变性、`check_balance.ts` 的重评/年龄/世界护栏，以及可追溯矩阵探针。加入新候选前应保留现有断言，并核对其实际样本、数据与源码版本。

## 不能直接合入的旧代码与测试

- 必须保留线上 `src/engine/me/auto.ts` 的 `practiceWithRoom()`、每次训练前动态重查和枪法突破次数保护。旧bal版本会恢复固定尾项空练；线上已有 `check_auto_training_room.ts` 的16组聚焦检查和40条短配对。
- 保留线上存档/IndexedDB、多标签、撤回经济、身份资料修复、地图年代、LCQ、下一场、信箱和所有已上线UI。不得用旧数值树覆盖这些模块。
- 保留线上 `scripts/check_worldline.ts` 的真实比赛树受控正例，不退回bal的“换seed4碰冠军”。
- 保留线上 `scripts/check_trophies.ts` 的赛事/队名括号识别及负例断言；bal旧版本会撤销这些测试修复。
- bal的 `check_hall` seed7→8只对旧数值候选做过解释与验证，不能未经复验直接替换当前夹具。
- `package.json` 只追加必要检查，不覆盖当前完整检查链；更新日志只追加新内容，不能拷贝旧整文件。
- 不回滚 `src/data/*`。线上刚修复的生日、国籍、姓名、队名与赛制资料应保留；旧报告的静态评分结果需要在当前资料上复核。

## 老档迁移与指挥加成的缺口

### 1. 当前线上老档没有被旧迁移测试覆盖

两树的 `RULER` 均为2。`migrateRuler()` 对已stamp=2或已有 `rulerMoved` 的档直接返回；`rereadWorld()` 也会跳过已在尺子上的档。现有 `check_ruler_migrate.ts` 主要删除stamp来模拟更早的旧档，并不是 `3addd0f → 新数值规则` 的迁移测试。

同时，`ensureCeilings()` 只在没有 `p.caps` 时创建上限；`CAP_BASE 68→70` 不会自动重算现有caps。冠军补发还有 `bn.rev===2` 的门槛，因此不能认为提高BREAK_VALUE后旧奖杯自动取得相同新奖励。

实施前应明确哪些规则从下一次行动/冬歇起生效，哪些需要有版本标记的一次性迁移。不能仅把RULER加一就复用旧“全量加shift”的迁移，否则可能重复应用旧调整。至少需要当前线上实际格式、已经迁移过的档、不同年份、主角已破瓶颈/已夺冠的档，以及重复加载不再改变的回归。

### 2. 新IGL加成有同步风险

bal新增 `IGL_BONUS=3`，综合与上限依赖 `isIgl`；但现有 `takeIgl/revokeIgl/iglDrop` 只切换身份标记，没有立即调用综合与上限重算。移植后应定向验证任命、撤回、离队、读档的缓存一致性，避免加成迟到或离任后残留。

此项是静态代码可以定位的集成风险，本审查没有运行动态复现，不冒称已有完整bug复现结果。

## 旧方案不是单纯加速成长

| 常数 | 线上3addd0f | bal候选 |
|---|---:|---:|
| CAP_BASE | 68 | 70 |
| SOLO | 1.6 | 1.8 |
| MECH_VALUE_MAX | 1.2 | 1.4 |
| MILE_VALUE_MAX | 3 | 5 |
| CAP_EXP_MAX | 5 | 6 |
| 联赛冠军突破 | 0.8 | 1.2 |
| 大师赛突破 | 1.6 | 3 |
| 全球冠军赛突破 | 2.4 | 4.5 |
| 决赛MVP突破 | 1 | 2 |

bal还把反应衰退提前到24岁、枪法25岁，33岁训练价值归零。17–19岁的训练更快，但23岁、25岁等年龄段的训练价值低于线上；成熟老档可能下一冬就承受更早、更强的新衰退。其训练曲线与奖励增长也会共同改变何时进入强队、何时夺冠以及何时打开更高上限。

**更早衰退来自作者此前“年轻提升快、老了慢、最终会下滑”的历史设计，不是本轮解决成长慢所必需的修法。** 本轮ROOM_SCALE实验应保留线上年龄行为，不能把年龄衰退、训练加速和冠军奖励一次混改，再从终局上升或下降猜是哪项起效。

旧矩阵里的“保持SOLO1.8、CAP_BASE70”是保持旧数值分支自身参数，不是保持线上现值。对当前线上应用它们分别就是1.6→1.8、68→70的实质平衡改动。

## 仍需测量的成长机制

- 托管空练已有上线修复，不能再将旧 `[first,vod,aim]` 无效尾项当作未修根因。
- 当前仍有可检验的差异：练习组合与位置权重、实际训练AP、属性上限附近的 `roomMul` 减速、俱乐部训练focus、替补/无队经历、设施和教练、冠军突破反馈。
- `primaryFocus()` 依据已执行练习次数选择俱乐部训练方向，平手按ACTIONS顺序；个人行动可能同时改变额外训练与俱乐部训练。这是待量化机制，不等于已证实位置偏置bug。
- `hourValues()` 是即时名义收益指标，不是考虑突破、比赛机会和长期状态后的最优策略。不能简单把最高小时值托管当作无条件正确答案。
- 旧20条矩阵没有完整动作/XP/突破分账，不能从终局快照倒推每项机制的贡献。

## 本轮最小实施与验证建议

1. 从已上线3addd0f建同源对照，保留全部生产修复。第一项仅比较 `ROOM_SCALE=10` 与 `8`；其余常数、年龄、世界规则与托管政策固定。
2. 使用有效出身键 `netcafe`，记录实际解析后的出身；五位置与相同种子交叉，先固定职业起点/赛区，再扩展起点，避免把赛区和出身混杂。
3. 输出逐属性累计XP/属性收益、到顶浪费、行动AP、突破进度、年龄、上场机会、设施暴露及机械/经验/冠军奖励分账；不要只看终局综合。记录初始、逐年、终点、实际结束原因和源码/参数哈希。
4. 保留当前属性上限与已获成就，先确认该实验不会让无空间属性积存XP、越过上限、破坏天赋差异或突破路径；ROOM_SCALE的作用也不是全档等比例加速，应按剩余空间分组观测。
5. 尺子封顶/赛事背景、队友重评以后作为独立实施单元，连同专用回归在当前资料上验证，不用成长实验替代。
6. 年龄及IGL若未来启用，先补老档和职务变动回归，并明确不追溯扣除历史成长。最后合并候选再统一执行完整检查和发布审批。

### 旧探针口径更正

旧bal探针传入 `originKey='net'`，这不是有效键；`originOf()` 会回退到 `ORIGINS[0]`，即 `netcafe`。旧配对在同一回退规则下仍有诊断价值，但新实验应显式校验有效键，不能将回退掩盖为正常配置。

`emptyTalents()` 实际返回20点均衡预设（枪法/反应/意识/道具各3，其余四项各2），不是零天赋。新报告应称“均衡天赋”；此前口头“空天赋”不准确。

## 证据文件

- 主仓 `转交-2026-09-20-接手说明.md` 最新上线状态及历史数值章节。
- bal-tune `数值核验-2026-09-20.md`：严格封顶修复、局部回归及87–89目标未完成。
- bal-tune `数值先导矩阵-2026-09-20.md` 与对应20条JSON：旧源码341866f的固定EMEA先导，不能冒充当前线上基线。
- 线上版本的 `玩家反馈首批-2026-09-20.md`、`check_auto_training_room.ts`、`probe_training_efficiency.mjs`：已上线空练修复及其有限范围的40条26周配对。

审查完成即停止；后续实现与模拟由主agent单独调度，本审查不占用重型模拟槽。
