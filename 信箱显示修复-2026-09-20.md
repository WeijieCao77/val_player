# 信箱长文显示修复

本地完成，未推送。作者已批准其他完成项上线；当前发布候选锁在 `4f4ef74` 的独立工作树，本修复不自动混入该次发布。

## 改动与验证

根因是信箱正文继承通用单元格的 `white-space: nowrap`，原来的 `max-width: 0` 不能保证内容留在列内。现改为固定列宽，列表最多两行，长文省略号；点建议或“查看全文”在其下展开跨四列的完整文本。

全文保留换行并允许连续长英文换行；纯文本渲染，不解释玩家提供的 HTML。点赞和状态仍独立，点击点赞不会误开详情。Escape 收起详情并返回入口；首页信箱需要再按一次 Escape 才会关闭外层卡片。

刷新、切换分类、成功投稿都清除旧展开状态，避免重建列表后把焦点交给已经卸载的旧按钮。

- `npm run typecheck`、`git diff --check` 通过。
- `node scripts/check_mailbox_mobile.mjs`：320/390/1440 × 首页/生涯，共 6 场景全部通过。
- 覆盖长中文、无空格英文、emoji、换行、HTML 不执行、本人待审核/隐藏状态、点赞、键盘、刷新和提交；使用合成列表，没有向线上投稿/投票或读取玩家存档。
- 12 张截图和几何检查报告在 `.cache/mailbox-mobile`。
- 已接入本地候选的 `check:feedback`，但不影响另一个工作树中正在验证的 `4f4ef74`。

## 后台合并 ID 在哪里

在 [信箱审核页](https://val-player-production.up.railway.app/dash/box)每条建议的“状态”列，日期下一行的 `#xxxxxxxx` 就是 **8 位建议 ID**；不是公开榜名次，也不是旁边的设备短编号。

要将 A 合并到 B：复制 B 的 8 位 ID（不带 `#`），填进 A 那一行的“并到 id”，再点 A 的合并按钮。公开榜保留 B 的正文和状态，A 的赞去重并入 B；两段正文不会拼接。**本轮新机制**将 A 留作仅原作者和审核后台可见的合并回执，不在公开榜重复展示。上线前仍是下文说明的旧机制，不要把本地完成当作线上已生效。

作者随后已授权实现合并回执；已修改本地后台和前端，尚未改变任何线上审核状态或执行线上合并。

## 上线前的旧机制

A 并到 B 后，刷新会让 A 从原作者的“我的”列表消失；没有“已合并”状态或去向链接。若 B 已公开，原作者可以在公开榜看到 B，原有投票已合入并去重，正常显示为已投票；但 B 不会因此变成“你提的”（除非 B 本来也是同一设备提交）。B 未公开时，A 的原作者不会因此获得查看 B 的权限。

## 作者批准后的新机制

- 公开榜只保留合并后的建议；来源原文、原提交时间和“已合并”状态留在本人“我的”中。
- 点开原建议，能看到“你的建议已合并至同类建议”。公开目标可再点开查看编号、全文、票数、已展示/已采纳/已修复状态，不依赖目标是否在公开榜前 200 名。
- 目标尚未公开，只说明暂未公开，不返回正文、编号或设备信息；目标已删除/无有效链路，则说明暂不可查看，原文回执仍保留。
- 连续合并跟随最终目标，票数去重；原回执不再接受投票、重复合并或改回普通公开状态。
- 每次打开或刷新信箱读取最新状态；不新增自动投稿、自动点赞、后台轮询或通知权限。
- 历史版本已经删除的合并来源不自动复活。新机制只保证此后合并的回执，不能承诺恢复旧记录。
- 回执也占用 600 条总上限，合并不保证释放容量；后台删除仍是真的删除，请谨慎使用。

专用后端与真实 UI 回归见 `check_box_merge.mjs` 和 `check_box_merge_ui.mjs`。最终验证及发布状态以当前批次结果为准。

## 本地整合验证

`npm run check:feedback` 与 `npm run build` 均退出 0，日志 `review-new-ui-feedback.log` / `review-new-ui-build.log`。包括已有反馈修复、信箱长文、支持入口、一次性结局提示、下一场边界、合并后端和 6 个合并 UI 场景；合并 UI 生成 24 张截图，已抽查手机长文和结局提示。类型检查及构建通过，仅有现有大块资源体积警告。

旧信箱、容量、坏尾回归另行通过。新回执已覆盖压实/重启、写入/重命名失败、连续合并后删除中间回执、目标掉出榜单前 200、超过 50 条个人记录仍能查看早期回执、投票超限拒绝且不丢票、私密目标不泄漏。此处不是声称新功能已跑完整 `check:me`，也不是已上线。
