# 无畏契约选手生涯模拟器 · 总体策划稿 v0

2026-09-05。**讨论稿**，不含代码。

依据：`Val_Manager`（无畏契约电竞经理，React/TS，引擎约 2 万行，524 名真实选手）与
`LOL_breaker`（破晓 · LoL 选手生涯模拟器，原生 JS，含两份策划稿与一份交接待办）。
两个仓库都完整读过一遍，引用处标了文件和行号，后面动手时可以直接去抄。

---

## 〇、一句话

**用 Val_Manager 的引擎和真实数据，做 LOL_breaker 那种「一个人的职业生涯」。**

你是一名虚构的无畏契约新人选手。从排位/Challengers 起步，进 VCT 四大赛区的真实俱乐部，
在真实的 Kickoff → Masters → Champions 日历里争首发、谈合同、处理更衣室，
直到退役。比赛是逐回合模拟出来的，你在关键回合做决定；赛场外有粉丝、直播、钱和人。

---

## 一、两个前作各自的长处与短板

### 1.1 长处对照

| 维度 | Val_Manager（经理） | LOL_breaker（选手） | 新项目取谁 |
|---|---|---|---|
| **比赛引擎** | 逐回合模拟：每回合按枪法/反应/残局加权抽击杀、首杀、助攻、伤害、残局（`match.ts:480-570`）；地图整图状态偏移 `MAP_SWING=6`、回合坡度 `ROUND_SENS=30`（`match.ts:31,33`）；经济/半场/加时/暂停/战术指令全有 | 每局一个节点决策 → `swing` → sigmoid 胜率，没有回合概念 | **Val_Manager 的引擎为骨，LOL_breaker 的节点决策为肉**（见第七节） |
| **数据** | 78 队 / 524 真人 / 121 新人 / 1676 场真实赛事 / 523 张档案照；`build_world.py` 从 vlr.gg 分位映射出 8 维，可复现 | 2022 一年数据拟合 5 维，S13 之后世界靠推演；指挥值与战队实力 +0.52 伪相关 | **Val_Manager** |
| **赛制** | 完整 VCT 日历（`season.ts:56-105`）、Challengers/Ascension、两套 ruleset（2025/2026 抽签版 `draw.ts`）、三败/双败/瑞士轮/GSL 模板（`bracket.ts`） | 春夏两赛段 + MSI + 世界赛，5 个赛季 | **Val_Manager** |
| **世界演化** | 按天推进；年龄曲线、退役两段式、AI 转会、新秀池、升降级、`rebaseSeasonClock`（`season.ts:1743-2035`） | `ageWorld()` 有一条很好的教训：不给 NPC 成长，「世界停在原地等你」（`career_template.html:1575`） | Val_Manager 的机制，LOL_breaker 的标定思路 |
| **人际** | 三轴分得干净：忠诚(对俱乐部) / 信任(对经理) / 羁绊(队友之间，对称、积怨复利)（`loyalty.ts` `trust.ts` `bonds.ts`） | 队友信任、教练/经理信任、话语权、`checkMateExit` 的「预警 → 后果」因果链（`team.js:57`） | 两边合并：Val_Manager 的数据结构 + LOL_breaker 的因果链 |
| **选手视角循环** | 无（经理视角） | 周 + 行动点；职业前一年 20 周；试训四天；首发竞争；`nextWeek` 钩子清单（`career_template.html:3961`） | **LOL_breaker** |
| **赛场外** | 俱乐部商务（Gig/Venture/赞助/联盟分成）——选手视角用不上 | 粉丝/热度两层收敛模型（第三版才对）、直播三方博弈、48 条事件 + 气质轴特质 + 待办、一键托管四档 | **LOL_breaker**，逐个移植 |
| **结局/成就** | 架构好：两条轨道各出一个结局；条件只对存档提问不写 flag（`endings.ts:21-26`） | 内容好：14 种结局、86 项成就、生涯名片截图 | Val_Manager 的架构，LOL_breaker 的内容量 |
| **UI** | React 组件齐（MatchLive 逐回合观战、Bracket、Standings、PlayerModal、真人照片 Dossier）；三套主题 | 叙事优先：`pinbar` 常驻属性条、叙事/数值双模式、把公式印在界面上（`squadCard`）、遮罩 vs 内联的区分、按钮上现算成本 | 两边都要 |
| **测试** | 78 个审计脚本：无头整季 + 不变量 + 批量统计 + 可达性证明（`check_reachable.ts`） | 一个 `playOne` 机器人 + 批测方法学 + 「尺子没接上」的教训 | Val_Manager 的体系，LOL_breaker 的验收口径 |

### 1.2 各自的坑（新项目第一天就要防）

来自 LOL_breaker 的交接文档，全是实测出来的：

1. **「写好了但没接上」** —— 项目头号 bug 形态。`relMod()` 没人调、`BREAK_PATHS.心态` 的 `done()` 写死 false、`AP_OFF` 从没被用过、72 个队标躺着没导出、`checkAch("worlds")` 从未调用。**每一次 replace 都写断言。**
2. **「量它的尺子没接上」** —— 测试机器人走了和界面不同的分支，导致「外赛区 0%、MSI 38%」三条结论全错（真值 36% / 85%）。**机器人必须走界面同一条派发路径。**
3. **一个数字扛五件事必然溢出** —— 旧 `fame` 同时当曝光/资产/收入乘数/门槛/成就条件，生涯末 100% 顶格。
4. **累加模型衡量的是你玩了多久** —— 粉丝要「朝战绩决定的天花板收敛」，第三版才做对。
5. **钱没有出口** —— 生涯末资金中位 2078 万、支出 ≈ 0；且出口不能转化成属性。
6. **加内容必然通胀** —— 多出 30 个行动点把 MSI 冠军率从 19% 抬到 45%，回调三处才回去。**每加一段可玩内容跑一次 120 局批测。**
7. **一局只有目标时长的一半，且 43% 点击是重复训练** —— 加周数只会更枯燥，要加的是分叉。

来自 Val_Manager 的注释和审计：

8. **`myTeam` 是全局特权分支** —— 至少 40 处 `teamId === state.myTeam`。选手版里语义大多仍成立（「我效力的队伍值得算细」），但要逐处过一遍。
9. **常量两边不一致就对不上账** —— `SALARY_BASE`/`ROLE_WEIGHT` 在 `player.ts` 和 `build_world.py` 必须逐位相同，注释里记着曾经 94 人重算掉 3 分。
10. **结局条件写字符串常量自我验证等于没验证** —— `'VALORANT Champions'` vs `'Champions'` 让 8 个结局永久不可达，`check_reachable.ts` 就是为它写的。

---

## 二、定位与体验目标

### 2.1 玩家是谁

- 一名 **17–19 岁的虚构新人**，进入由真实选手构成的世界。（LOL_breaker 同样是虚构主角 + 真实名单，可行。）
- 视角是**一个人**：我练什么、我打得怎样、教练让不让我上、队友信不信我、钱怎么花、粉丝怎么涨。俱乐部的账本、赞助、董事会一概不归我管。
- 目标层次：上首发 → 打进 Masters/Champions → 拿冠军 → 成为「这个项目的门面」→ 体面退役。

### 2.2 一局体验

| 项 | 目标 |
|---|---|
| 生涯长度 | 最多 10 个赛季（2026–2035），或到退役；第 5 季末有一次「收官/续」的选择（沿用 Val_Manager 的五年之约） |
| 一局时长 | 2–3 小时（LOL_breaker 目标，实测只到一半——要靠分叉不靠周数） |
| 决策密度 | 每周 3–5 个真决定 + 每场比赛 5–8 个节点 + 每周 ≤1 个事件 |
| 重复操作占比 | ≤ 25%（LOL 实测 43%） |
| 二周目 | 出身/起点/赛区/时代变量不同；成就分单局与跨周目 |

### 2.3 设计铁律

从两个前作提炼出来的，写在这里是为了以后每一条新系统都对着检查：

1. **把公式写在脸上。** 战力、赢面、训练收益、成本，全部现算现显示，不写死数字（LOL `squadCard` / `costBits`）。
2. **一个数字只扛一件事。** 热度 ≠ 粉丝 ≠ 咖位；能力 ≠ 状态；忠诚 ≠ 信任 ≠ 羁绊。
3. **每个选项都有得有失。** 事件、特质、合同条款、独家直播——没有纯赚的项。
4. **成败对称。** 试训、节点、谈判的失败代价不打折；同时防「故意输」的激励。
5. **决策后立刻给赢面。** 「赢面 43% → 51%」必须每次都显示；劣势局里做对了也要看得见变化（`clampWinProb` 的滑动封顶）。
6. **凡是能由真事引出的，不摇骰子。** 爆冷、连败、夺冠、队友离队后的事件是定向触发；随机池只放真的随机。
7. **引擎不碰浏览器 API。** 脚本开头 stub 掉 `localStorage/fetch` 就能无头跑完十年——这是审计体系存在的前提。
8. **机器人走界面同一条路径。** 所有推进/派发逻辑只有一份，UI 和测试都调它。
9. **真人数据留空不编造。** 缺的字段显示「未知/推算」，不补假名字。
10. **每加内容跑一次批测。** 结局分布、冠军率、钱中位、粉丝档位覆盖，四个数不许漂。

---

## 三、技术方案

### 3.1 结论：fork Val_Manager 的引擎，重写「谁做决定」这一层

理由：Val_Manager 有约 5000 行经过 78 个审计脚本标定过的引擎 + 1.4 MB 不可替代的真实数据；
`arena.ts`（卡牌模式）已经证明这套引擎可以被非经理模式借用——拼一个临时 `GameState` 就能调 `simulateMatch`。
LOL_breaker 的价值在**设计与教训**，代码本身耦合 LoL 结构和全局 `S`，不搬。

代价：TypeScript + React + Vite 的工程门槛比 LOL_breaker 的「零依赖一个 HTML」高；
但 `vite build` 产物同样是静态文件，Railway 部署方式不变。

### 3.2 从 Val_Manager 拿什么（按改动量分级）

| 级别 | 模块 | 说明 |
|---|---|---|
| **原样搬（<10% 改动）** | `rng.ts` `match.ts` `comp.ts` `agents.ts` `player.ts` `bonds.ts` `trust.ts` `loyalty.ts` `bracket.ts` `league.ts` `draw.ts` `ruleset.ts` `qualify.ts` `prospects.ts` `content.ts` `nat.ts` `imports.ts` `save.ts`(pack/unpack/配额三级) `theme.ts` `styles.css` `ui/common.tsx` 以及整个 `src/data/` 与 `scripts/` 抓取链 | 与视角无关 |
| **骨架留、判定换** | `training.ts`（focus 来源从「经理排」改成「教练排 + 我加练」）、`transfer.ts` 的 `scoreOffer`（视角反转：我评估报价）、`season.ts` 的 `advanceDay`（拆成 `advanceWorld()` + `advanceMe()`）、`endSeason`、`life.ts`（五个日常事件改成发生在我身上）、`endings.ts`/`achievements.ts`（架构留、条件全换）、`selectLineup`（改成教练 AI） | 第 4–12 节逐个说 |
| **丢弃** | `finance.ts` `commercial.ts` `leagueShare.ts` `staff.ts`（俱乐部经营层，只留 `PRIZE` 表和 `bonusShare` 分成）、`career.ts` `manager.ts`（留 `skillMod` 接口和 `scoutedPotential` 的「信息不完全」机制）、`agenda.ts` `actions.ts`、`gacha.ts` `cards.ts` `market.ts` `inbox.ts` `cardActions.ts`（卡牌模式）、服务端全部 | v1 纯本地存档 |

`GameState` 保留骨架，把经理侧字段（`manager, managerContract, jobOffers, jobApplications, tenures, boardConfidence, objective, staff, gigs, ventures, sponsorTalks, leagueDeal, tally`）换成选手侧字段，新增 `me: string`。

### 3.3 目录（草案）

```
src/
  engine/          ← fork 自 Val_Manager，去掉经理层
    world/         season, league, bracket, draw, transfer(AI 侧), prospects, life
    match/         match, comp, agents  + nodes.ts(新：我的节点)
    me/            新：week, actions, growth, starter, contract, fans, stream, money, events, traits, quests, auto
    people/        bonds, trust, loyalty, coachAI(新)
    save, rng, endings, achievements, types
  ui/              React；标签结构见第十三节
  data/            world.json / dossier.json / records.json / prospects.json（原样）+ origins.json / events.json / nodes.json（新）
scripts/           check_*.ts 审计 + bot.ts（无头机器人，走 UI 同一条派发）
```

---

## 四、核心循环

### 4.1 两个时钟

- **世界按天走**（沿用 `advanceDay` 的世界部分）：赛程、AI 转会、伤病、新闻。
- **我按周做决定**（LOL_breaker 的周 + 行动点）：周初分配行动点，点「推进」，世界跑 7 天；
  遇到我队的比赛停下打比赛，遇到需要我拿主意的事停下弹窗。

这样 VCT 真实日历（周四/周日赛日、Masters 后 14 天间歇）原样保留，玩家又只需要按周思考。

### 4.2 行动点

沿用 LOL_breaker 的「按现实精力定价」：轻 1 = 碎片时间，中 2 = 半天，重 3 = 整个下午。

| 预算 | 值 |
|---|---|
| 赛季周 | 8 |
| 休赛期周 | 8（休赛期必须真的可玩——LOL 的 `AP_OFF` 写了没接上） |
| 职业前周 | 12 |
| 伤病中 | 4 |

| 行动 | 点 | 主要效果 | 来源 |
|---|---|---|---|
| 枪法训练（Aim Lab / DM） | 2 | aim / reaction xp | 训练 |
| 复盘（VOD） | 2 | awareness / clutch xp；若是指挥则 igl | 训练 |
| 道具/跑图 | 2 | utility / teamwork；地图熟练（`mapPrefs`） | 训练 |
| 排位（Radiant 天梯） | 1 | 保手感、全维小涨、段位、状态向中性拉 | LOL `solo` |
| 训练赛（队伍安排，可选参与度） | 3 | 阵容熟练度 `familiarity`、羁绊、教练信任 | LOL `scrim` + VM `learnComp` |
| 队友双排 | 1 | 指定队友羁绊 +、沟通 xp | VM `duo` |
| 直播 | 2 | 热度、钱、疲劳 +、触发平台事件 | LOL |
| 做内容（vlog/剪辑/采访） | 2 | 热度（不同方向） | LOL `content` |
| 休息 | 1 | 疲劳 −、tilt 泄压 | LOL |
| 对位挑战（替补时） | 2 | 首发竞争资本 `edge` | LOL `duel` |
| 社交（家人/朋友/队友聚餐） | 1 | 心态、羁绊、事件线 | 新 |

规则：1 点行动必须做收益折算（LOL `lightMul=0.50`，不配平会被刷）；
训练收益每周结算走 Val_Manager 的 `trainPlayer` 公式（第 6.2 节）。

### 4.3 一周流程

```
周初  ┌ 常驻：HUD（身份 / 现在在哪 / 资源） + pinbar（8 维 + 状态 + 疲劳）
      ├ 本周牌：教练安排（训练赛/合练日）、赛程（对手、赢面）、待办（quest 期限）
      └ 分配行动点 → 「推进」
推进  ┌ 世界跑一天……
      ├ 我队比赛日 → 赛前（阵容公布：我上不上）→ BP → 逐回合 + 节点 → 赛后（数据/归因/复盘）
      ├ 事件（遮罩三选一，带「按推荐」）
      └ 转会/合同/试训/独家（内联卡但滚顶）
周末  └ 结算：训练 xp、疲劳回落、状态、羁绊/信任周结、粉丝/热度、财务、伤病判定、自动存档
```

推进的自动停止规则照抄 LOL 的 `autoStop`：只跳过「没得选的周」，任何阻塞项出现立刻交回玩家。

---

## 五、生涯阶段

### 5.1 开局

| 项 | 内容 | 来源 |
|---|---|---|
| 出身 | 18 张背景卡三选一（可重抽），换算口径「资金 ≈ 1 点、人气 10 ≈ 1 点、属性 1 = 1 点」，**刻意不等值，差在形状** | LOL `origins.js`，内容换成 VAL：CS 转型、网吧、主播、韩服天梯、校园战队、Premier 冠军队、退役选手之子…… |
| 天赋 | 20 点分到 8 维（上限 `55 + t*4`）+ 心态/体质各一档 | LOL `TOTAL_TALENT` |
| 赛区 | 中国（默认）/ Pacific / Americas / EMEA。影响语言、联赛、粉丝盘 | VM 四赛区 |
| 位置 | 决斗者 / 先锋 / 控场 / 哨卫；可后天练第二位置（`rolePro`） | VM |
| 起点 | ① 排位/Premier 起步（长）② Challengers 青训（中）③ T1 替补（短） | 新；回应「一局太短」和「有人只想打比赛」 |

### 5.2 阶段 A · 职业前（起点 ①，v2 做）

对应 LOL 的「一年 20 周」：Radiant 天梯（`RANK_CURVE` 一条折线把 8 维均值映射到天梯分，
国服第一 = 天梯前 1）、Premier 赛季、城市赛/主播邀请赛（抽 4 个路人队友，默契从头练）、
青训教练私信 → 到场 → 试训邀请三条通道（战绩 / 段位 / 人气，`tryout.js:92/140/282`）。
`tierCard` 把「各档俱乐部要什么水平的人、你差多少」两把尺子同时摆出来。

### 5.3 阶段 B · Challengers（T2）

Val_Manager 的每赛区 7–8 支二级队是真实的（含 121 名真实新人）。
Challengers 1/2 + Ascension 升降级原样沿用。这一层的戏是：**T2 的钱是 T2 的钱**（LOL 经济重锚），
Ascension 夺冠整队升 T1 是最大的翻身机会；T1 队来挖你是另一条路。

### 5.4 阶段 C · VCT（T1）

Kickoff → Stage 1 → Masters → Stage 2 → Masters → Champions → 休赛期。
两套 ruleset 都保留：`vct-2025` 经典，`vct-2026` 带抽签仪式（`draw.ts` 已实现，
抽签结果由存档种子锁定，玩家只负责揭球——不能读档刷弱组）。

### 5.5 外赛区（LOL 的「上下两向」直接可用）

- 往上：打得好 → 三个外赛区豪门来挖，语言课影响权重但不挡门。
- 往下：**低谷退路**——打不上首发时，别的赛区的 T2/弱队给首发位。「只有向上的路」是 LOL 最初的缺陷。
- 差异化：语言（课程终于有用）、时差、家人不在身边 → 心态与默契；粉丝在当地涨国内掉（`fanCap ×0.82`）。

### 5.6 退役与结局

年龄驱动（沿用 `season.ts:1937` 的退役概率曲线：33+ 45%、31+ 20%、29+ 6%），
可主动宣布「这是最后一季」（`retiring`，一年告别期）。也可以被没人签约逼退。
复出机制（LOL `ageWorld` 的传奇池）作为彩蛋。

---

## 六、能力与成长

### 6.1 维度

**引擎 8 维不动**（比赛引擎和 524 人的数据都建在上面）：
枪法 aim / 反应 reaction / 意识 awareness / 道具 utility / 残局 clutch / 协同 teamwork / 沟通 communication / 指挥 igl。

对玩家呈现时按训练方向分四组（枪法线 / 意识线 / 团队线 / 指挥线），`pinbar` 上仍显示 8 个数。

**新增两个「体质型属性」**（0–100，不进 `overall`，作为乘子和门槛）：

| 属性 | 影响 | 怎么练 |
|---|---|---|
| 心态 | tilt 抗性（LOL `tiltDrag`：55 以上开始拖累）、大赛加成 `stageBonus` 的兑现率、落后局节点成功率 | 落后局赢球、运动心理课、休息、更衣室稳定 |
| 体质 | 疲劳恢复速率、伤病风险、连轴国际赛的衰减 | 休息、理疗、作息事件、年龄 |

NPC 也有这两项（按 `hashStr('body:'+id)` 生成，老将体质低、指挥心态高），伤病判定对称。
**这是要讨论的点之一**：也可以不加、只用 form/fatigue 表达。我倾向加，因为「心态」是选手叙事的核心词。

### 6.2 训练收益

以 Val_Manager 的 `trainPlayer`（`training.ts:65-124`）为底，套 LOL 的行动点和瓶颈：

```
gain = rng(7,16) × age(≤20:1.35 / ≤23:1.1 / ≤26:0.8 / 0.45)
     × tired(疲劳>70:0.5 / >45:0.8 / 1) × motivated(0.75 + 士气/200)
     × (1 + 教练development + 设施) × clamp(空间/12, 0.25, 1.6)
     × 我投入的行动点份额 × 装备/课程/特质乘子 × 版本红利(本季关键维 ×1.1)
xp[attr] += gain，满 100 → +1
```

上限 = 天赋上限 + 里程碑池（≤7）+ 机械池（≤6）+ 经验（每赛段 +0.3，≤3）——LOL 的分池设计，
理由是「肯做就有」的不能先把池子占满，让 Champions 奖杯只剩 +0.7。

### 6.3 瓶颈与突破（LOL `BREAK_PATHS` 改造）

破法写在明面上；只有**顶着瓶颈**才算突破；用**本周实际发放的预算**审判；声明式补发不靠瞬间触发。

| 维度 | 破法 |
|---|---|
| 枪法/反应 | 连续 3 周每周 ≥3 点投枪法线 |
| 意识/残局 | 一个赛段做满 6 次复盘，或打出 3 次 1v2+ 残局 |
| 道具/协同/沟通 | 一个赛段训练赛全勤 + 阵容熟练度 ≥80 |
| 指挥 | 与 IGL 搭档一个赛段 / 队伍战力 ≥80 / 打进国际赛（任一） |
| 心态 | 落后局赢 3 场，或拿一次冠军 |
| 体质 | 一个赛段休息满 12 次 |

### 6.4 状态、疲劳、伤病、年龄

全部沿用 Val_Manager：`form` 回归 70（`training.ts:348+`）、疲劳每周 `−(fatigue×0.30+5)`、
伤病 `risk = (0.001 + load×(0.018 + max(0,age−27)×0.002)) × grace`、赛后二次判定、
理疗 35 疲劳/次、`ageDrift`（24 岁巅峰，28 起衰退，枪法/反应掉得快）、`seasonRollover` 的潜力重估。

LOL 的一条补充：**指挥随年龄涨**（`DECAY_W.指挥 = −0.30`）——Val_Manager 已有「25 岁以上 igl 50% 概率 +1」，一致。

状态显示沿用 LOL 的五档文案（状态爆棚 / 手感不错 / 正常 / 状态下滑 / 低谷），
并把「能力 80 / 状态 55 = 正在低谷的强者」这句写进界面。

---

## 七、比赛

这是两者结合最关键的一节。

### 7.1 引擎

Val_Manager 的 `MapSim` 逐回合跑，不改。每回合：

```
strA = atk/def + BUY_MOD[buy] + callMod + swingA
p = 1 / (1 + exp(−(strA − strB) / 30))
```

击杀按 `(42 + (aim×0.55 + reaction×0.3 + clutch×0.15)×0.78) × KILL_WEIGHT[role] × (0.9 + form/500)` 加权抽样，
所以我的 ACS / K/D / 首杀 / 残局都是**真打出来的**，不是赛后合成（LOL 是合成的，`synthSeriesStats`）。

### 7.2 我的节点（LOL `nodes.js` 移植到回合级）

引擎里已有两个现成的钩子：回合强度的 `swingA/swingB`，和战术指令 `TacticalCall.focus`
（指定选手击杀权重 ×1.55，`match.ts:505`）。节点成功 = 给我方一个正 swing + 对我 focus 三回合；失败 = 负 swing。

```
v  = 0.7 × 我的该维 + 0.3 × 队友同维均值            // 一个人拉不动四个人
p  = clamp(0.30 + v/100 × 0.55 − (risk − 0.5) × 0.15, 0.12, 0.92)
swing += (ok ? +1 : −1) × risk × NODE_SWING         // NODE_SWING 初值 4，批测标定
```

节点池按局势过滤（`when(ctx)`），一图 2–3 个，一场 5–8 个，同场不重复：

| 场景 | 例子 | 吃哪维 |
|---|---|---|
| 手枪局 | 「对面五人堆 A，指挥问你敢不敢 rush B」 | reaction / igl |
| 经济局 | 「队友让你拿唯一一把枪，还是五人一起 force」 | teamwork / aim |
| 首杀（决斗者/先锋） | 「烟还没起，先手还是等道具」 | reaction / utility |
| 残局 1vX | 「1v2 拆包局：打、藏、拆」 | clutch / awareness |
| 落后（≤5–11） | 「暂停，所有人看着你」 | 心态 / igl |
| 赛点 / 加时 | 「对面赛点，指挥把最后一波交给你」 | 心态 / clutch |
| 国际赛 | 「观众声浪比联赛大一个量级」 | 心态 |
| 状态 | 「今天手感烫得离谱 / 怎么打都不对」 | aim / 心态 |
| 换人 | 「教练问要不要下场换替补」（仅当有 6 人） | 心态 |

**每次决策后立刻显示本图赢面变化**：由当前比分和回合胜率 p 闭式算「先到 13」的概率
（负二项分布尾部，加时另算），不需要蒙特卡洛。
劣势封顶用 LOL 的 `clampWinProb` 滑动封顶：`GAP_WINDOW` 之外上限随差距滑动，做对了永远算数。

节点账本 `nodeLog`（当时成功率 / 结果 / 动了几个点）写进比赛记录，复盘和成就都读它。

### 7.3 BP、英雄、战术

- 图 ban/pick 与英雄单由**教练 + IGL** 决定（`runVeto` 的 AI 分支）。我的参与度由话语权决定：
  新人只被告知；主力可以提议；核心/IGL 可以拍板。
- 我的英雄池 `agentPool` 是真练出来的：`rolePro` 每 34% 送一个英雄；不对位英雄 −12% 发挥（`agents.ts`）。
- 战术滑杆（pace/utility/aggression/adaptability）是教练的，我看得见但改不了，除非我是 IGL。

### 7.4 赛后

三层，全部现算：

1. **个人数据**：K/D/A、ACS、ADR、首杀/首死、残局、评分——同一张 boxscore 里取，队友和球探看的是同一份。
2. **归因**（LOL `postmatch.attribute` + VM `EdgeBreakdown`）：基础战力 / 指挥 / 默契 / 教练 / 阵容 / 地图 / 道具 / 我的节点，
   按 `|v|` 排序列出——**每一项就是模拟器判定时真正用到的数**。
3. **复盘建议**（LOL `reviewAdvice`）：长项先说、短板按「权重 × 差距」排、给净账；极端加点明说没有短板惩罚。

「院长局」（输球但我评分全队最高）单独播报并计入转会评分。

### 7.5 观战 / 跳过 / 托管

同一个 `MapSim`。观战 `TICK_MS=420` 逐回合（`MatchLive.tsx`）；跳过时节点由「按推荐」自动选；托管同理。
坐替补时按周看结果，随队冠军标 ring（LOL `addRingTitle`）。

---

## 八、队伍与首发

### 8.1 教练 AI（替代经理）

Val_Manager 的教练已有真人姓名和 `tactics / development / motivation` 三个值。新增决策：

- **选人**：改写 `selectLineup`（`match.ts:87-139`）——现在是 `value = effectiveRating×0.15 + compositionScore`，
  改成 `教练视角评分 = 有效评分 + 教练信任×w + 位置熟练 + 阵容契合 − 不满 − benchLock`。
  我不能决定，只能影响。
- **排训练**：全队训练计划（跑图/复盘/练位置）由教练排，我按参与度吃收益，可额外加练。
- **换人**：LOL `demoteCheck`——连输三场且我是 `weakestLink` 才换；对手强 8 分以上那场不算。

### 8.2 首发竞争（LOL `rotation.js` 完整移植）

```
isBenched：8 维加权压过同位首发 −2 自动转正，除非 benchLock
训练赛对位挑战：p = 0.52 + (我−他)/38 − (risk−0.8)×0.10 − max(0,疲劳−55)×0.003 + (状态乘子−1)×1.2
  每周 ≤2 次；赢 BO3 → edge += 1(+0.5 亮眼)；edge ≥3 → 试用期进名单
试用：赢 → 永久转正；输给强 6 分以上的对手原谅一次；场次用完 → 回替补，资本减半
队友伤病：0.4%/人/周（27+ 加 0.2%，30+ 再加），伤的是我的对位 → 直接顶上
```

### 8.3 人际三轴（Val_Manager 结构 + LOL 因果链）

| 轴 | 方向 | 沿用 | 新增 |
|---|---|---|---|
| 羁绊 bonds | 我 ↔ 每个队友，对称 −100~100 | `initialBond`（共处月数/国籍/年龄/位置/协同沟通）、`applyMatchBonds`（赢球全员 +、输球只有 carry vs 拉胯才扣，积怨复利） | 双排、聚餐、事件选项直接改 |
| 信任 trust | 教练对我 / 经理对我（两个） | `trust.ts` 的周结算改成「我的行为 → 他们怎么看我」：疲劳过高、商务过多、直播过多、带伤训练、赛后甩锅 | 教练信任决定首发与 BP 参与度；经理信任决定合同与引援话语权 |
| 忠诚 loyalty | 我对俱乐部 | `LOYALTY_NEW=38`、年增 `(78−v)×0.16`、夺冠 +2.5/+5 | 影响我拒绝挖角时的心态、老东家回归 |

**队友离队因果链**（LOL `checkMateExit`）：羁绊 ≤ −40 的队友先给一次预警（「他已经不跟你说话了」，一个赛段挽回），
仍不改 → 真的走 → 全队信任 −9 → 默契砸 → 教练 −6。人走得有征兆才是后果。

更衣室事件带 `rec` 和气质轴；**每个选项都有得有失**。

### 8.4 话语权（LOL `clout.js`，刻意做得不好用）

```
clout = 18 + 赛区冠军×5 + 国际冠军×11 + clamp(粉丝/14,0,16) + clamp((我−队均)×0.9,−8,18) + 胜率项 + clamp((均信任−50)×0.16,−6,6)
档位：队魂 78 / 核心 62 / 主力 44 / 轮换 26 / 新人
```

挂牌队友：威望 ≥55 且教练信任 ≥68；点名引援：威望 ≥66 且经理信任 ≥72；失败扣经理信任。
LOL 的教训：这套代码写好了但界面没接上——**移植时先写可达性断言**。

---

## 九、合同与转会

### 9.1 视角反转的 `scoreOffer`

Val_Manager 的谈判核心（`transfer.ts:124-233`）每一项都保留贡献值以便说出真正的阻碍，
这个结构原样保留，但方向翻转：**我**评估几家俱乐部的报价。

| 项 | 我怎么看 |
|---|---|
| 薪资 vs 期望 | `expectedSalary` 公式沿用（`SALARY_BASE=33000 × exp((overall−55)/12)`，T2 ×0.14） |
| 承诺位置 | `deservedRole` 回答「我去这队能打首发吗」——直接复用 `roleFit` |
| 平台 | 声望差、T1/T2 升降、赛区 |
| 年限 / 签字费 / 分成 / 违约金 / 禁挖条款 | 沿用 `Contract` 字段 |
| 我的野心 / 忠诚 | `ambition`、`loyalty` |
| 经纪人 | 新：档次决定报价数量与谈判筹码 |

合同年限沿用 Val_Manager 的**错峰发放**（同队同年到期最多两人），这是俱乐部侧的现实约束，对我也成立。

### 9.2 试训四天（LOL `tryout.js` 的 VAL 版）

| 天 | 内容 | 考哪维 |
|---|---|---|
| 1 | DM / Aim 测试 | aim / reaction |
| 2 | 训练赛 | utility / teamwork |
| 3 | 复盘会 | awareness / igl |
| 4 | 经理面谈 | 心态 / communication |

`p = clamp(0.22 + (attr − 疲劳惩罚)/100 × 0.62, 0.08, 0.92)`，`delta = ±risk × 4.2`，**成败对称**；
`grade = (我的实力 + 试训分 + 战术素养) − 该档期望`：≥16 A+ / ≥8 A / ≥0 B / ≥−9 C / D。

俱乐部四档（豪门 / 中游 / 弱队 / 青训）的期望值和薪资区间按 Val_Manager 的真实 `budget`/`reputation` 重标，不用 LOL 的手填表。

### 9.3 谈判四问（LOL `DEAL_ASKS`）

加薪 25% / 签字费翻倍 / 缩短年限 / 违约金 −40%，各有 cost；
底气 = 评级 + 粉丝 + 段位 + 履历（「天梯分在谈判桌上不值钱，打过的比赛才值钱」）。
谈崩不是立刻失败，先降条件。**拒签是合法策略**，代价是这家今年不再来。

### 9.4 转会

- 两段式：赛段结算 `noteScoutInterest`（「看台上出现了 XX 的教练」）→ 转会窗兑现 `rollProOffers`。
- 转会窗沿用 Val_Manager 的 `TRANSFER_WINDOWS = [[0,20],[63,90],[165,198],[323,363]]`。
- 评分 `proPerf`：8 维均值差 ×3 + 队伍胜率 ×0.55 + 场均评分 + 院长局 + 冠军 + 状态 + 粉丝 − 坐板凳 + 训练赛数据 − 违约金拖累。
- **冠军没人问在现实里不存在**：国际冠军 p=1，赛区冠军 p≥0.96；perf≥13 连两窗没人问保底 0.92。
- **主动侧**（LOL 待办 #3）：休赛期挂牌自己 / 自荐 / 接触；成功率吃 `cloutOf` 和违约金。
- 问询频率目标 4–6 次/局（LOL 实测 13 次太吵）。
- AI 之间的完整转会市场沿用 Val_Manager 的 `aiTransferTick`（每周每队 10%）。

### 9.5 个人财务（新，不复用俱乐部财务）

收入：年薪（按周发）、奖金分成 `bonusShare`、直播、内容、代言事件、成就奖励。
支出：见第 10.3 节。经纪人抽成 5–10%。T2 的钱是 T2 的钱。

---

## 十、赛场外

### 10.1 粉丝 / 热度（LOL 第三版模型，原样搬）

```
一切「涨名气」涨的都是热度；heat 每周 ×0.90
fanCap = 120 + 天梯×1.5 + 杯赛×… + 赛区冠军×520 + Masters×1150 + Champions×2300 (+独家 180)；外赛区 ×0.82
fanWeek: rate = (职业 0.012 : 职业前 0.055) + clamp(heat/9000, 0, 0.045)，再乘内容/特质/推流
         fans += (cap − fans) × rate            // 朝天花板收敛，不累加
咖位七档由粉丝决定；门槛类读粉丝，收入类读粉丝×热度
```

验收：咖位落在最高档 ≤15%，覆盖 ≥5/7 档；粉丝与战绩相关（无冠 / 赛区冠军 / Masters / Champions / 双冠 单调递增）。

### 10.2 直播三方博弈（LOL `shop.js:203-226`）

每家俱乐部按 hash 定一个合作平台。独家谈判三选一：

| 选择 | 签字费 | 保底 | 俱乐部抽成 | 经理信任 |
|---|---|---|---|---|
| 签俱乐部合作平台 | ×0.8 | ×1.0 | 20% | +6 |
| 签对家平台 | ×1.4 | ×1.15 | 40% | −12 |
| 不签 | — | 随粉丝×热度浮动，上限最高下限最低 | 0 | — |

开播条款：签了独家每赛段最少播 2–3 次，做不到赔钱。分成档位上调时发事件告诉玩家。

### 10.3 钱的出口（LOL v2 未做的那部分，这里做进 v1 的一半）

原则：**出口转化成粉丝 / 关系 / 事件 / 风险，不转化成属性**。

| 出口 | v1 | v2 |
|---|---|---|
| 外设 | 5 槽，效果只到第 2 档，第 3 档是收藏 | — |
| 课程 | 语言（外赛区才生效）、运动心理、复盘方法、沟通 | — |
| 放松 / 理疗 | 花钱换体力，不占行动点 | — |
| 经纪人 | 三档，决定报价数量与谈判筹码 | — |
| 家用 | 出身自带 upkeep 的有对应解法 | 给父母买房、家人事件线 |
| 住处 | — | 宿舍 → 合租 → 房子 → 别墅，疲劳恢复与事件 |
| 团队 | — | 剪辑团队（涨粉转化率）、营养师、心理医生 |
| 投资 | — | 网咖、战队股份、理财——**会亏钱** |

验收：生涯末资金中位 ≤ 收入的 40%；「全买光」的总价 ≥ 生涯收入中位。

### 10.4 事件、特质、待办、世界痕迹（LOL 全套）

- 事件池目标 **60+ 条**，每条 `{rec, w, max, auto, when, q, a[{t, g, e}]}`；
  四类：梗与文化（「VCT 圣经」、语录鬼畜、解说外号）、人生（家人生病、老同学、机场被堵）、
  行业（转会爆料、平台跳槽、拍广告）、触发式（夺冠后 / 爆冷后 / 连败后 / 破纪录后 / 队友离队后）。
  伏笔链沿用：职业前留 flag，职业后兑现。
- **选前拍快照、选后 diff**，把变化摊开给玩家看。
- **气质轴 → 永久特质**：硬 / 暖 / 苦 / 秀，同轴 5 次得特质，互为反面的排他，每个有得有失：
  刺头（涨粉 ×1.18、信任涨幅 ×0.80、逆风 +2.2）/ 更衣室粘合剂 / 劳模 / 话题人物。获得那一刻用一整屏。
- **待办 quest**：事件留下有期限、有进度、有代价的任务；进度由 `noteAct` 顺手统计。
  它真正改变的是这一周的行动点怎么花。
- **世界痕迹**：某队官宣裁人 → 名单真的换；互喷约战 → 写进宿敌；跟教练要轮换 → 真的坐一场。

### 10.5 一键托管（LOL `auto.js` 四档）

采购 / 商务 / 日常 / **生涯（默认关且带警告）** + 总开关 + 每个遮罩右下角「按推荐」。
两条规矩：**不收钱**（收钱等于惩罚这种玩法）；**不是黑箱**（每次执行留痕）。
代价是隐性的：推荐永远走中庸路线——**精简版能打出不错的结果，完整版才有上限**。
验收：全托管的冠军率比机器人手动低 5–15 个百分点。

---

## 十一、赛季、世界与版本

### 11.1 日历与赛制

原样沿用 `season.ts` 的 364 天日历与 `LEAGUE_DAYS`、`INTERNATIONAL_OPEN`、`BREAK_AFTER_INTERNATIONAL=14`。
LOL 待办 #5「国际赛之间要有间隔和文字日志」在这里天然满足；抽签系统提供「小组分配」的过程日志。

### 11.2 世界演化

沿用 `endSeason`：升降级、合同倒计时、AI 续约（`overall ≥ rating−6 && 72%`）、退役两段式、
新秀补充（121 名真人 + `makeProspect`）、`rebaseSeasonClock`。
LOL 的标定要求：**NPC 年轻人要真的成长**（不然「世界停在原地等你」），
Val_Manager 的 `check_ai_growth.ts` 已经在守这条（30–50% 空间兑现）。

### 11.3 版本

- 图池：沿用 `activePool(seed, phase)`，赛季三个窗口各换 1–2 张。
- **版本强势位置**（LOL `SEASONS[].fav/dim`）：每赛季一个 `meta = {favRoles, keyAttr}`，
  强势位置的选手发挥 +、本季关键维训练 ×1.1、版本变更事件。
- 英雄削弱/加强事件影响 `agentPool` 里具体英雄的对位系数。

### 11.4 时代变量（v2，留存）

每周目随机 1–2 条本局规则，开局明示：「这个版本节奏极快，枪法权重更高」「转会市场冷清」
「豪门更愿意用新人」。**改的是规则不是数值大小**，复杂度不掉。

---

## 十二、结局、成就、留存

- **架构**照 Val_Manager：两条轨道各出一个结局（荣誉线 × 故事线），条件只对存档提问（honours / clubHist / titles / 节点账本），不写 flag。
- **内容**照 LOL：荣誉线 ≈ 14 种（破局者 = Masters+Champions 双冠 / 王朝 / 世界冠军 / 无冕之王 / 四强遗恨 / 赛区功勋 / 板凳冠军 / 常青树 / 泯然众人 / 没能上岸……），
  故事线（一生一队 / 流浪 / 出海 / 回国 / 主播转型 / 教练转型……）。
- **成就 80+**，分单局 `run` 与跨周目 `life` 两个 scope；刻意分散在互斥路径上（有的要专精有的要均衡，有的奖励和平共处有的奖励把人挂牌）。
- **生涯名片**（LOL `careerPoster`）：结局大字 + 奖杯墙 + 十年一行 + 五个数 + 签名，为截图设计。
- **可达性证明**：`check_reachable.ts` 那套——每个结局/成就必须真跑出来过或用收割素材拼得出。
- 留存：传承（解锁项而非数值）、难度选项、成就跨周目累计——v2。

---

## 十三、界面

标签结构照 LOL（玩家已验证过的），组件用 Val_Manager 的 React：

| 栏 | 内容 | 组件来源 |
|---|---|---|
| 本周 | 行动点、教练安排、赛程与赢面、待办、推进 | 新 + `Dashboard` 的议程 |
| 我的 | 8 维 + 心态/体质、状态曲线、个人数据、比赛档案、生涯、外设 | `PlayerModal` `Dossier`（真人照片同款卡） |
| 队伍 | 队友（羁绊图）、教练/经理信任、默契、战术滑杆（只读）、话语权 | `Squad` `TacticSliders` |
| 赛事 | 赛程、积分榜、对阵图、抽签、各赛区新闻 | `Schedule` `Standings` `Bracket` `DrawCeremony` |
| 转会 | 试训邀请、报价、挂牌、门槛卡、合同 | `Transfers` 改造 |
| 新闻 | 电竞周报 + 生涯大事记 | `Digest` |
| 经济 | 收入/支出、外设、课程、直播合同 | 新 |
| 成就 | — | `Achievements` |
| 托管 | 单独一栏、隔一条线、冷灰色 | 新 |

规矩：
- `pinbar` 常驻属性条（sticky 在 HUD 下）。
- 「需要你做决定」的卡一律遮罩；内联卡出现时滚到内容区顶部（`blockSig` 只算内联）。
- 叙事/数值双模式：默认用档位词（世界级 / 顶尖 / 职业级……）和箭头，数值一个不改一个不藏。
- 按钮上现算成本与收益（`costBits`）。
- 移动优先：560px 断点，底栏 4 主入口 + 「更多」抽屉。
- 三套主题沿用。

---

## 十四、测试与验收口径

### 14.1 体系（Val_Manager 的四类 + LOL 的机器人）

1. 无头整局机器人 `scripts/bot.ts`：**走和 UI 同一条派发函数**，`playOne(opts)` 返回统计。
2. 不变量：每次推进后检查（每队 ≥5 人、starters=5、双向指针、我在且只在一队、行动点不为负、合同年限一致……）。
3. 批量统计：120 局看结局分布、冠军率、钱中位、粉丝档位覆盖、决策点数、崩溃数。
4. 可达性：每个结局/成就/事件/特质至少被真跑出来一次。
5. 反馈完整性：任何实质状态变化必须有一条对玩家可见的 note（`audit_feedback.ts` 的 diff 法）。
6. 每一次 replace 写断言。

### 14.2 首版验收目标（初值，跑出基线后再定）

| 指标 | 目标 |
|---|---|
| 崩溃 | 0（400 局） |
| 一局决策点 | ≥ 1200（LOL 实测 763 ≈ 51–102 分钟） |
| 重复训练点击占比 | ≤ 25% |
| 赛区冠军 / Masters / Champions / 双冠 | 60–70% / 30–40% / 15–25% / 8–12%（从 T1 替补开局，全生涯） |
| 首发率（第一季 / 第三季） | 40–60% / ≥85% |
| 转会问询 | 4–6 次/局 |
| 咖位最高档 / 覆盖 | ≤15% / ≥5 档 |
| 外赛区效力 | 15–30% |
| 钱中位 / 收入中位 | ≤ 40% |
| 事件覆盖种类 | ≥ 25 种/局 |
| 托管 vs 手动冠军率差 | 5–15 个百分点 |

---

## 十五、分期计划

| 期 | 内容 | 产出 |
|---|---|---|
| **P0 · 引擎落地** | fork Val_Manager，删经理层，`GameState` 加 `me`，`advanceDay` 拆成 world/me，把「我」注入一支 T1 俱乐部当替补，无头跑完一季不崩 | `bot.ts` 能跑；不变量脚本通过 |
| **P1 · 可玩内核** | 周循环 + 行动点 + 训练/瓶颈 + 比赛节点 + 赢面显示 + 赛后归因 + 首发竞争 + 教练 AI 选人 + 人际三轴 + 合同/转会（视角反转）+ 赛季/退役/结局 + 存档 + 最小 UI | 从 T1 替补开局能打完十年；120 局基线出来 |
| **P2 · 赛场外** | 粉丝/热度、直播三方、个人财务与出口 v1、事件 60+、气质轴特质、待办、世界痕迹、托管四档、成就 80+、生涯名片 | 验收表第二批指标 |
| **P3 · 起点与深度** | 职业前阶段（天梯/Premier/杯赛/试训三通道）、Challengers 起点、2026 抽签 ruleset 接入、版本 meta、外赛区上下两向 | 三种起点都可玩 |
| **P4 · 留存** | 时代变量、传承、难度、跨周目成就、发布（Railway / B 站 Toy） | — |

P0–P1 是一个整体，做完才有东西可玩；P2 之后每一项都可以独立上线。

---

## 十六、需要你拍板的问题

1. **技术栈**：fork Val_Manager（TS/React/Vite）——我的推荐；还是像 LOL_breaker 一样零依赖单 HTML？后者要重写引擎，代价大。
2. **能力维度**：8 维 + 心态/体质两个体质型属性（我的推荐）；还是只保留 8 维，用 form/fatigue 表达心态体质？
3. **起点**：v1 只做「T1 替补开局」（最短路径验证核心循环），职业前和 Challengers 起点放 P3——可以接受吗？
4. **赛区**：默认中国赛区起步、四赛区都可选？还是 v1 只做中国赛区？
5. **虚构主角进真实队伍**：我会挤掉一名真实首发（LOL_breaker 就是这样）。接受？还是我永远占「第 6 人」名额、只有靠竞争才顶掉真人？我倾向后者，更有戏。
6. **生涯长度**：10 季（Val_Manager 的 2026–2036）还是 5 季（LOL）？我倾向 10 季 + 第 5 季末的收官选择。
7. **时间粒度**：周 + 行动点（我的推荐）；还是直接用 Val_Manager 的按天推进？
8. **联机**：v1 纯本地存档；生涯档案/排行榜以后接 Val_Manager 的服务端。同意？
9. **名字**：破晓的兄弟项目？还是独立命名？
10. **比赛节点密度**：一图 2–3 个 / 一场 5–8 个是初值，太密会烦。你对 LOL 版的节点密度感受如何？

定下 1、2、3、5、7 五条，就能开 P0。
