# 玩家反馈首批候选：做到对阵图为止

状态：本地实现和定向验收中，未推送、未部署、未改变线上信箱审核状态。整批完整回归结果待下方补录。工作目录为 `.claude/worktrees/review-batch`，分支 `codex/review-batch`；不要从外层旧工作树发布。

## 本次范围

作者批准优先级表前几项，到杯赛／季后赛树状图为止。实现托管无效训练修复、成长受控诊断、保存失败提示与回归、实时生涯入口、对阵图。自定义头像及其后的功能不混入本批。创意工坊仍只策划，电竞卡牌肉鸽已经独立。

### 1. 托管不再练无收益项目

固定训练尾项原来没有重新判断训练空间。可复现状态中，枪法和反应均为 80/80、突破已耗尽、无剧情/任务时，仍花 2 AP 练枪，属性和经验都没有收益。

现在每次个人训练前重新检查：保留仍能提升任一相关属性的项目；否则换有效训练或跳过。指挥学习、突破、剧情、训练任务、复盘减压、双排关系和替补训练赛用途不会被一刀切掉。

原复现训练安排为复盘 2 次、排位 6 次、枪法 1 次，疲劳 38；修复后复盘 3 次、排位 6 次，疲劳 33。成长倍率和潜力上限没有调整。

`scripts/check_auto_training_room.ts`：16 组聚焦回归，覆盖部分满值、动态练满、突破、任务、天赋、5 角色、职业/业余、疲劳、行动点、关系/信任、确定性与随机数无额外消耗。

### 2. 成长慢：先修浪费，未宣称整体平衡完成

在同一份当前引擎/资料上，仅把 `auto.ts` 分别换成基线 `9a327a3` 与本次候选进行配对。不是拿不同数据版本比较。

- 2026 年、中国、网吧出身、天梯开局、均衡天赋；5 个位置 × 2 个固定种子 × 2 种状态 × 修前/修后 = 40 条、每条 26 周，共 1,040 周。
- 普通开局的 10 对：报告中的最终属性、经验、潜力、年龄、队伍、疲劳和冠军数等快照完全相同；平均综合增长均为 2.9。
- 人工“枪法/反应已封顶且突破额度用完”的压力状态 10 对：平均综合增长从 1.9 变成 2.3；4 对最终综合提高 1，其余 6 对综合不变，但属性/经验等快照有差异。
- 这说明本修复解决部分无效安排，不说明“成长慢”已全面解决。样本短、起点单一，压力状态非自然生涯分布；没有测出达 90 所需年龄、完整职业寿命或夺冠率，不能据此改全局倍率。

正式结果：`.cache/training-efficiency-20260920-final.json`，包含两份自动训练源码哈希、完整构建哈希和逐条快照。复跑命令：`node --expose-gc scripts/probe_training_efficiency.mjs run <新的结果路径>`。脚本默认只打印计划，拒绝覆盖旧结果，禁止外网并使用内存存储；低于 1.5 GB 可用内存自动停止。

一次重跑触发过内存保护，保留不完整原始结果而未视为通过；加上逐样本垃圾回收后正式 40 条全部完成。旧 `training-efficiency-20260920.json` 使用未知出身键回退，不作为正式结果。

下一步成长专项应覆盖不同出身/地区/职业起点和完整生涯，再提交有数字依据的倍率或瓶颈方案。独立 `bal-tune` 候选没有合并。

### 3. 保存可靠性

本次没有新增存储引擎或迁移。保留已有 IndexedDB 大存档、失败时导出内存中最新进度、多标签所有权保护；补充“电脑空闲内存不等于浏览器存储额度”“先导出、不要清除网站数据”提示。

`check_save_room`、`check_save_tabs` 定向通过：覆盖数据库拒写、配额不足、指针写入失败、迁移失败保留旧副本、刷新、导出最新内存状态、多标签/退出页面异步竞争。新 `check_save_notice_mobile` 在 320/390/1440 检查重试、导出入口、折叠重开及页面边界。

没有玩家原始存档与浏览器环境，不能承诺该名玩家的个案已被百分百复现或解决。

### 4. 我的·生涯

- 入口更名“我的·生涯”，顶部新增累计/当季切换；地图数、正式赛出场、评分、ACS、K/D、击杀/死亡/助攻、胜率、整场 MVP、首杀差。
- 读取现有累计账本，不从可能已裁剪的最近比赛列表反推。无比赛/零分母显示“—”；正式职业赛与业余杯赛、训练赛不混算。
- 当前年份的本人冠军分“有出场”与“未出场随队”；资格赛出线不当冠军；可跳到奖杯和能力区。历史赛季表保留。
- 比赛数据完赛结算后可见；冠军以奖杯账本入账为准，逐日模式可能要再推进一次才同步，不宣称决赛画面关闭就必定显示。没有为展示调用会触发剧情/奖励的同步方法。
- 名片显示时清除原始实名中的 HTML 注释污染，原始资料不改写。

纯读取回归、320/390/1440 × 空档/有比赛/当季替补 9 个真实 MeScreen 场景通过；18 张截图在 `.cache/career-overview-mobile`。

### 5. 对阵图

只读参考作者 [Val_Manager 的 Bracket 实现](https://github.com/WeijieCao77/Val_Manager/blob/main/src/ui/Bracket.tsx)。没有改经理游戏或把经理界面/管理功能接到玩家入口。

玩家原组件只覆盖旧 KO/SW 标签，真实赛历页面未接入。新图读取当前生涯实际赛历的晋级节点，胜者实线、败者虚线，待定不填未来历史结果。循环/瑞士按轮次展示，不伪造淘汰关系；兼容双败、三败、GSL 和已记录的旧单败赛。可横向滚动、点有赛报的比赛查看详情。

业余杯赛引擎不保存全体参赛队签表，因此展示“你的个人晋级路线”，不虚构其他队伍和比赛。未晋级后的轮次明确没有出场。

对阵图数据与浏览器验证已通过：2021–2027 共 588 个事件、14,467 节点、10,981 条晋级边；320/390/1440 × 6 场景共 18 个真实界面场景，无整页横溢，能滚动到后续轮次。未来/已结束赛事默认折叠，详见《玩家对阵图-2026-09-20.md》。

## 整批验证与发布边界

- 新 `npm run check:feedback` 已接入 `check:me`，包含自动训练、生涯总览、对阵图和三个界面检查。
- 首批源码已本地提交 `9122c20`；该轮完整 `check:me` 因作者追加“2022 年出现天枢云阙”而中断，日志 `review4-check-me.log` 不能当全绿。优先完成地图补丁后，包含本批的新候选统一重验。前批 `a022216` 的全绿不能代替本批。
- 还包括先前已提交、未发布的选手身份修复 `9a327a3` 与信箱容量/2022 南美 LCQ 修复 `a022216`。选手审计的资料覆盖和残留不确定项见三份分项报告；不是声称所有选手资料都经过逐人联网认证。
- 上线前由作者审批。没有推送任何分支、修改线上反馈状态或读写玩家存档。
